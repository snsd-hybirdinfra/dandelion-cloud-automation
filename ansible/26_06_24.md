> Note: 본 문서는 작성 당시 진행 상황을 기준으로 작성된 작업 기록이다.
> 최신 아키텍처 기준은 `docs/current-status.md` 및 `docs/architecture.md`를 따른다.

---
<!-- STATUS: DRAFT / ?ㅼ젣 ?ㅽ뻾 諛?寃利???-->

# Phase 1 Ansible ?먮룞???묒뾽 ?뺣━
## HAProxy 濡쒕뱶諛몃윴??援ъ꽦遺??諛깆뾽 ?먮룞?붽퉴吏

## 1. 臾몄꽌 紐⑹쟻

蹂?臾몄꽌??Team Dandelion ?꾨줈?앺듃?먯꽌 湲곗〈 `docker.yml`, `database.yml`, `wordpress.yml` ?묒꽦 ?댄썑 吏꾪뻾???묒뾽???뺣━?쒕떎.

?ㅻ뒛 吏꾪뻾??二쇱슂 ?묒뾽? ?ㅼ쓬怨?媛숇떎.

```text
HAProxy 怨듯넻 蹂??異붽?
??haproxy.cfg Jinja2 ?쒗뵆由??묒꽦
??proxy.yml ?묒꽦
??HAProxy 肄붾뱶 援ъ“ 諛??ㅽ뻾 ?먮쫫 ?ㅻ챸
??backup.yml ?ㅺ퀎 諛??묒꽦
??MariaDB 諛?WordPress ?뚯씪 諛깆뾽 ?먮쫫 援ъ꽦
???듭떖 Playbook ?묒꽦 ?꾨즺 ?щ? ?뺤씤
```

?꾩옱 Playbook? 肄붾뱶 ?묒꽦 ?④퀎?대ŉ ?ㅼ젣 ?몃뱶 ?곸슜怨??ㅽ뻾 寃利앹? ?꾩쭅 吏꾪뻾?섏? ?딆븯??

---

# 2. ?ㅻ뒛 ?묒뾽 ???곹깭

湲곗〈源뚯? ?묒꽦???뚯씪? ?ㅼ쓬怨?媛숇떎.

```text
playbooks/
?쒋?? common.yml
?쒋?? docker.yml
?쒋?? database.yml
?붴?? wordpress.yml
```

?쒕퉬??援ъ꽦? ?ㅼ쓬怨?媛숇떎.

| ?몃뱶 洹몃９ | ?쒕퉬??| ?ㅼ튂 諛⑹떇 |
|---|---|---|
| `proxy` | HAProxy | Ubuntu ?⑦궎吏 吏곸젒 ?ㅼ튂 |
| `web` | WordPress | Docker 而⑦뀒?대꼫 |
| `db` | MariaDB | Ubuntu ?⑦궎吏 吏곸젒 ?ㅼ튂 |
| `backup` | 諛깆뾽 ?뚯씪 ???諛?寃利?| Ubuntu ?뚯씪 ?쒖뒪??|
| `monitoring` | 紐⑤땲?곕쭅 | ?댄썑 ?뺤옣 ?④퀎 |

?꾩옱 Inventory 湲곗? 二쇱슂 ?몃뱶???ㅼ쓬怨?媛숇떎.

```ini
[proxy]
proxy-node ansible_host=192.168.4.32

[web]
web1-node ansible_host=192.168.4.4
web2-node ansible_host=192.168.4.35

[db]
db-node ansible_host=192.168.4.201

[backup]
backup-node ansible_host=192.168.4.134
```

---

# 3. HAProxy 怨듯넻 蹂??異붽?

?뚯씪 ?꾩튂:

```text
~/dandeliondir/ansible/group_vars/all.yml
```

異붽? ?댁슜:

```yaml
# HAProxy ?ㅼ젙
haproxy_frontend_port: 80
haproxy_backend_port: 80
haproxy_balance_algorithm: roundrobin
```

蹂?섎퀎 ??븷:

| 蹂??| ??븷 |
|---|---|
| `haproxy_frontend_port` | ?ъ슜?먯쓽 ?붿껌??諛쏅뒗 Proxy ?몃뱶 ?ы듃 |
| `haproxy_backend_port` | HAProxy媛 Web ?몃뱶濡??붿껌???꾨떖???ы듃 |
| `haproxy_balance_algorithm` | Web ?몃뱶 ?붿껌 遺꾩궛 諛⑹떇 |

?꾩옱 ?곌껐 援ъ“:

```text
?ъ슜??
??Proxy Node:80
??web1-node:80 ?먮뒗 web2-node:80
```

---

# 4. HAProxy ?ㅼ젙 ?쒗뵆由??묒꽦

## 4.1 ?뚯씪 ?꾩튂

```text
~/dandeliondir/ansible/templates/haproxy.cfg.j2
```

## 4.2 ?묒꽦 ?댁슜

```haproxy
# Ansible?먯꽌 ?먮룞 ?앹꽦??HAProxy ?ㅼ젙 ?뚯씪

global
    log /dev/log local0
    log /dev/log local1 notice
    chroot /var/lib/haproxy
    stats socket /run/haproxy/admin.sock mode 660 level admin
    stats timeout 30s
    user haproxy
    group haproxy
    daemon

defaults
    log global
    mode http
    option httplog
    option dontlognull
    option forwardfor
    option http-server-close

    timeout connect 5s
    timeout client 50s
    timeout server 50s
    timeout http-request 10s
    timeout http-keep-alive 10s

frontend wordpress_frontend
    bind *:{{ haproxy_frontend_port }}
    default_backend wordpress_backend

backend wordpress_backend
    balance {{ haproxy_balance_algorithm }}
    option httpchk GET /

{% for host in groups['web'] %}
    server {{ host }} {{ hostvars[host].ansible_host }}:{{ haproxy_backend_port }} check inter 3s fall 3 rise 2
{% endfor %}
```

## 4.3 ?ㅼ젣 蹂???덉긽 寃곌낵

?꾩옱 Inventory? 蹂?섍컪???곸슜?섎㈃ backend ?ㅼ젙? ?ㅼ쓬怨?媛숈씠 ?앹꽦?쒕떎.

```haproxy
backend wordpress_backend
    balance roundrobin
    option httpchk GET /

    server web1-node 192.168.4.4:80 check inter 3s fall 3 rise 2
    server web2-node 192.168.4.35:80 check inter 3s fall 3 rise 2
```

## 4.4 ?듭떖 ?ㅼ젙 ?ㅻ챸

### frontend

```haproxy
frontend wordpress_frontend
    bind *:80
    default_backend wordpress_backend
```

Proxy ?몃뱶??紐⑤뱺 ?ㅽ듃?뚰겕 ?명꽣?섏씠?ㅼ뿉??TCP 80踰??ы듃濡??붿껌??諛쏄퀬 `wordpress_backend`濡??꾨떖?쒕떎.

### roundrobin

```haproxy
balance roundrobin
```

?뺤긽 ?곹깭??Web ?몃뱶???붿껌???쒖꽌?濡?遺꾩궛?쒕떎.

```text
?붿껌 1 ??web1-node
?붿껌 2 ??web2-node
?붿껌 3 ??web1-node
?붿껌 4 ??web2-node
```

### Health Check

```haproxy
option httpchk GET /
```

HAProxy媛 Web ?몃뱶??`/` 寃쎈줈??HTTP ?붿껌??蹂대궡 ?곹깭瑜??뺤씤?쒕떎.

```haproxy
check inter 3s fall 3 rise 2
```

| ?듭뀡 | ?섎? |
|---|---|
| `inter 3s` | 3珥?媛꾧꺽?쇰줈 ?곹깭 ?뺤씤 |
| `fall 3` | 3???곗냽 ?ㅽ뙣?섎㈃ ?μ븷 泥섎━ |
| `rise 2` | 2???곗냽 ?깃났?섎㈃ ?뺤긽 ?곹깭濡?蹂듦? |

### ?묒냽??IP ?꾨떖

```haproxy
option forwardfor
```

Web ?쒕쾭??`X-Forwarded-For` ?ㅻ뜑瑜?異붽??섏뿬 ?ㅼ젣 ?묒냽??IP瑜??꾨떖?쒕떎.

---

# 5. proxy.yml ?묒꽦

## 5.1 ?뚯씪 ?꾩튂

```text
~/dandeliondir/ansible/playbooks/proxy.yml
```

## 5.2 ?묒꽦 ?댁슜

```yaml
---
- name: Proxy ?몃뱶 HAProxy 濡쒕뱶諛몃윴??援ъ꽦
  hosts: proxy
  become: true
  gather_facts: true

  tasks:
    - name: Web ?몃뱶 洹몃９ 援ъ꽦 ?뺤씤
      ansible.builtin.assert:
        that:
          - groups['web'] is defined
          - groups['web'] | length > 0
        fail_msg: "Inventory??web 洹몃９ ?먮뒗 Web ?몃뱶媛 ?놁뒿?덈떎."
        success_msg: "HAProxy backend濡??ъ슜??Web ?몃뱶瑜??뺤씤?덉뒿?덈떎."

    - name: HAProxy ?⑦궎吏 ?ㅼ튂
      ansible.builtin.apt:
        name:
          - haproxy
        state: present
        update_cache: true
        cache_valid_time: 3600

    - name: Proxy ?몃뱶?먯꽌 Web ?몃뱶 HTTP ?묐떟 ?뺤씤
      ansible.builtin.uri:
        url: "http://{{ hostvars[item].ansible_host }}:{{ haproxy_backend_port }}"
        status_code:
          - 200
          - 301
          - 302
        follow_redirects: none
        return_content: false
      loop: "{{ groups['web'] }}"
      register: web_backend_check
      retries: 5
      delay: 3
      until: web_backend_check.status in [200, 301, 302]

    - name: HAProxy ?ㅼ젙 ?뚯씪 諛고룷
      ansible.builtin.template:
        src: ../templates/haproxy.cfg.j2
        dest: /etc/haproxy/haproxy.cfg
        owner: root
        group: root
        mode: "0644"
        backup: true
        validate: "/usr/sbin/haproxy -c -f %s"
      notify: HAProxy ?쒕퉬???ъ떆??

    - name: 蹂寃쎈맂 HAProxy ?ㅼ젙 利됱떆 諛섏쁺
      ansible.builtin.meta: flush_handlers

    - name: HAProxy ?쒕퉬???쒖꽦??諛??쒖옉
      ansible.builtin.systemd_service:
        name: haproxy
        state: started
        enabled: true

    - name: HAProxy ?ы듃 ?묐떟 ?湲?
      ansible.builtin.wait_for:
        host: 127.0.0.1
        port: "{{ haproxy_frontend_port }}"
        timeout: 60

    - name: HAProxy ?ㅼ젙 臾몃쾿 ?뺤씤
      ansible.builtin.command:
        cmd: haproxy -c -f /etc/haproxy/haproxy.cfg
      register: haproxy_config_check
      changed_when: false

    - name: HAProxy 寃쎌쑀 WordPress HTTP ?묐떟 ?뺤씤
      ansible.builtin.uri:
        url: "http://127.0.0.1:{{ haproxy_frontend_port }}"
        status_code:
          - 200
          - 301
          - 302
        follow_redirects: none
        return_content: false
      register: haproxy_http_result
      retries: 10
      delay: 5
      until: haproxy_http_result.status in [200, 301, 302]

    - name: HAProxy 踰꾩쟾 ?뺤씤
      ansible.builtin.command:
        cmd: haproxy -v
      register: haproxy_version_result
      changed_when: false

    - name: HAProxy 援ъ꽦 寃곌낵 異쒕젰
      ansible.builtin.debug:
        msg:
          - "{{ haproxy_version_result.stdout_lines[0] }}"
          - "{{ haproxy_config_check.stdout }}"
          - "Proxy ?몃뱶: {{ inventory_hostname }}"
          - "?묒냽 二쇱냼: http://{{ ansible_host }}:{{ haproxy_frontend_port }}"
          - "HTTP ?곹깭 肄붾뱶: {{ haproxy_http_result.status }}"

  handlers:
    - name: HAProxy ?쒕퉬???ъ떆??
      ansible.builtin.systemd_service:
        name: haproxy
        state: restarted
```

---

# 6. proxy.yml ?ㅽ뻾 ?먮쫫

```text
proxy 洹몃９ ?좏깮
??Inventory??Web ?몃뱶 ?뺤씤
??HAProxy ?⑦궎吏 ?ㅼ튂
??Proxy ?몃뱶?먯꽌 Web1/Web2 HTTP ?묒냽 ?뺤씤
??haproxy.cfg ?쒗뵆由?諛고룷
??諛고룷 ???ㅼ젙 臾몃쾿 寃??
???ㅼ젙 蹂寃???HAProxy 利됱떆 ?ъ떆??
??HAProxy ?쒕퉬???ㅽ뻾 諛?遺???먮룞 ?쒖옉
??Proxy ?몃뱶??80踰??ы듃 ?뺤씤
??HAProxy 寃쎌쑀 WordPress HTTP ?묐떟 ?뺤씤
??HAProxy 踰꾩쟾怨??묒냽 寃곌낵 異쒕젰
```

## 6.1 Web ?몃뱶 洹몃９ ?뺤씤

```yaml
groups['web'] is defined
groups['web'] | length > 0
```

Inventory??`web` 洹몃９??議댁옱?섍퀬 Web ?몃뱶媛 ??? ?댁긽 ?깅줉?섏뼱 ?덈뒗吏 寃?ы븳??

## 6.2 Proxy?먯꽌 Web ?몃뱶 吏곸젒 ?뺤씤

```yaml
url: "http://{{ hostvars[item].ansible_host }}:{{ haproxy_backend_port }}"
loop: "{{ groups['web'] }}"
```

?ㅼ젣 ?뺤씤 ???

```text
http://192.168.4.4:80
http://192.168.4.35:80
```

HAProxy瑜??ㅼ젙?섍린 ?꾩뿉 Proxy ?몃뱶? Web ?몃뱶 ?ъ씠???ㅽ듃?뚰겕 諛?HTTP ?곌껐???뺤씤?쒕떎.

## 6.3 ?ㅼ젙 ?뚯씪 ?ъ쟾 臾몃쾿 寃??

```yaml
validate: "/usr/sbin/haproxy -c -f %s"
```

?덈줈???ㅼ젙 ?뚯씪??`/etc/haproxy/haproxy.cfg`??諛섏쁺?섍린 ?꾩뿉 HAProxy 臾몃쾿??寃?ы븳??

臾몃쾿 ?ㅻ쪟媛 ?덉쑝硫????ㅼ젙???곸슜?섏? ?딄퀬 Playbook???ㅽ뙣 泥섎━?쒕떎.

## 6.4 Handler? flush_handlers

```yaml
notify: HAProxy ?쒕퉬???ъ떆??
```

`haproxy.cfg` ?댁슜??蹂寃쎈맂 寃쎌슦?먮쭔 ?ъ떆??Handler瑜??덉빟?쒕떎.

```yaml
ansible.builtin.meta: flush_handlers
```

?댄썑 ?ы듃 諛?HTTP 寃利??꾩뿉 ?ㅼ젙??利됱떆 諛섏쁺?섍린 ?꾪빐 ?덉빟??Handler瑜??ㅽ뻾?쒕떎.

## 6.5 HAProxy 寃쎌쑀 ?묐떟 ?뺤씤

```text
Proxy Node 127.0.0.1:80
??HAProxy frontend
??wordpress_backend
??web1-node ?먮뒗 web2-node
??WordPress ?묐떟
```

HTTP ?곹깭 肄붾뱶 `200`, `301`, `302`瑜??뺤긽?쇰줈 泥섎━?쒕떎.

---

# 7. HAProxy 寃??紐낅졊

臾몃쾿 寃??

```bash
cd ~/dandeliondir/ansible
ansible-playbook --syntax-check playbooks/proxy.yml
```

?ㅽ뻾 ????뺤씤:

```bash
ansible-playbook playbooks/proxy.yml --list-hosts
```

?덉긽 ???

```text
proxy-node
```

?ㅼ젣 ?ㅽ뻾 紐낅졊:

```bash
ansible-playbook playbooks/proxy.yml
```

?꾩옱 ?ㅼ젣 ?ㅽ뻾? 蹂대쪟 ?곹깭?대떎.

---

# 8. 諛깆뾽 怨듯넻 蹂??異붽?

?뚯씪 ?꾩튂:

```text
~/dandeliondir/ansible/group_vars/all.yml
```

異붽? ?댁슜:

```yaml
# 諛깆뾽 ?ㅼ젙
backup_remote_work_dir: "/var/backups/dandelion"
backup_storage_dir: "/srv/dandelion-backups"
backup_control_staging_root: "/tmp/dandelion-backup-staging"
backup_retention_days: 7
```

蹂?섎퀎 ??븷:

| 蹂??| ??븷 |
|---|---|
| `backup_remote_work_dir` | DB 諛?Web ?몃뱶?먯꽌 ?꾩떆 諛깆뾽 ?뚯씪???앹꽦?섎뒗 寃쎈줈 |
| `backup_storage_dir` | Backup ?몃뱶??理쒖쥌 諛깆뾽 ???寃쎈줈 |
| `backup_control_staging_root` | Control ?몃뱶???꾩떆 ?꾨떖 寃쎈줈 |
| `backup_retention_days` | 諛깆뾽 蹂닿? 湲곌컙 |

---

# 9. 諛깆뾽 ?꾩넚 援ъ“

愿由??몃뱶?쇰━ 吏곸젒 SSH ?묒냽?섏? ?딄퀬 Control ?몃뱶瑜?以묎컙 ?꾨떖 寃쎈줈濡??ъ슜?섎룄濡??ㅺ퀎?섏???

```text
DB Node ?????????
               ?쒋???Control Node ?꾩떆 ??????Backup Node
Web1 Node ???????
Web2 Node ???????
```

??諛⑹떇? ?꾩옱 Control ?몃뱶?먯꽌 紐⑤뱺 愿由??몃뱶濡??묒냽 媛?ν븳 Ansible 援ъ“瑜?洹몃?濡??댁슜?쒕떎.

---

# 10. backup.yml ?묒꽦

## 10.1 ?뚯씪 ?꾩튂

```text
~/dandeliondir/ansible/playbooks/backup.yml
```

## 10.2 Play 援ъ꽦

`backup.yml`? 珥??ㅼ꽢 媛쒖쓽 Play濡?援ъ꽦?섏???

```text
1. Control ?몃뱶?먯꽌 諛깆뾽 ?ㅽ뻾 ID ?앹꽦
2. DB ?몃뱶?먯꽌 MariaDB 諛깆뾽 ?앹꽦
3. Web ?몃뱶?먯꽌 WordPress ?뚯씪 諛깆뾽 ?앹꽦
4. Backup ?몃뱶??紐⑤뱺 諛깆뾽 ?뚯씪 ???諛?寃利?
5. Control ?몃뱶 ?꾩떆 ?뚯씪 ?뺣━
```

## 10.3 ?묒꽦 ?댁슜

```yaml
---
- name: 諛깆뾽 ?ㅽ뻾 ?뺣낫 以鍮?
  hosts: localhost
  gather_facts: false
  become: false

  tasks:
    - name: 諛깆뾽 ?ㅽ뻾 ?앸퀎???앹꽦
      ansible.builtin.set_fact:
        dandelion_backup_id: "{{ lookup('pipe', 'date +%Y%m%d_%H%M%S') }}"

    - name: Control ?몃뱶 ?꾩떆 諛깆뾽 ?붾젆?곕━ ?앹꽦
      ansible.builtin.file:
        path: "{{ backup_control_staging_root }}/{{ dandelion_backup_id }}"
        state: directory
        mode: "0700"


- name: MariaDB ?곗씠?곕쿋?댁뒪 諛깆뾽
  hosts: db
  become: true
  gather_facts: false

  vars:
    backup_id: "{{ hostvars['localhost'].dandelion_backup_id }}"
    control_staging_dir: "{{ backup_control_staging_root }}/{{ backup_id }}"
    database_backup_filename: >-
      wordpress-db-{{ inventory_hostname }}-{{ backup_id }}.sql.gz
    database_backup_path: >-
      {{ backup_remote_work_dir }}/{{ database_backup_filename }}

  tasks:
    - name: MariaDB 諛깆뾽 ?꾧뎄 ?ㅼ튂
      ansible.builtin.apt:
        name:
          - gzip
        state: present
        update_cache: true
        cache_valid_time: 3600

    - name: DB ?몃뱶 ?꾩떆 諛깆뾽 ?붾젆?곕━ ?앹꽦
      ansible.builtin.file:
        path: "{{ backup_remote_work_dir }}"
        state: directory
        owner: root
        group: root
        mode: "0750"

    - name: WordPress ?곗씠?곕쿋?댁뒪 ?ㅽ봽 ?앹꽦
      ansible.builtin.shell:
        cmd: |
          set -o pipefail
          umask 077

          mariadb-dump \
            --user=root \
            --protocol=socket \
            --socket=/run/mysqld/mysqld.sock \
            --single-transaction \
            --quick \
            --skip-lock-tables \
            --routines \
            --triggers \
            --events \
            --default-character-set=utf8mb4 \
            "{{ wordpress_db_name }}" \
            | gzip -9 > "{{ database_backup_path }}"
        executable: /bin/bash
      changed_when: true

    - name: MariaDB 諛깆뾽 ?뚯씪 ?뺣낫 ?뺤씤
      ansible.builtin.stat:
        path: "{{ database_backup_path }}"
      register: database_backup_stat

    - name: MariaDB 諛깆뾽 ?뚯씪 ?앹꽦 寃곌낵 寃利?
      ansible.builtin.assert:
        that:
          - database_backup_stat.stat.exists
          - database_backup_stat.stat.isreg
          - database_backup_stat.stat.size | int > 0
        fail_msg: "MariaDB 諛깆뾽 ?뚯씪???뺤긽?곸쑝濡??앹꽦?섏? ?딆븯?듬땲??"
        success_msg: "MariaDB 諛깆뾽 ?뚯씪???뺤긽?곸쑝濡??앹꽦?섏뿀?듬땲??"

    - name: MariaDB 諛깆뾽 ?뚯씪 ?뚯쑀沅?蹂寃?
      ansible.builtin.file:
        path: "{{ database_backup_path }}"
        owner: ubuntu
        group: ubuntu
        mode: "0600"

    - name: MariaDB 諛깆뾽 ?뚯씪??Control ?몃뱶濡?媛?몄삤湲?
      ansible.builtin.fetch:
        src: "{{ database_backup_path }}"
        dest: "{{ control_staging_dir }}/"
        flat: true
        validate_checksum: true
      become: false

    - name: DB ?몃뱶 ?꾩떆 諛깆뾽 ?뚯씪 ?쒓굅
      ansible.builtin.file:
        path: "{{ database_backup_path }}"
        state: absent


- name: WordPress ?뚯씪 諛깆뾽
  hosts: web
  become: true
  gather_facts: false

  vars:
    backup_id: "{{ hostvars['localhost'].dandelion_backup_id }}"
    control_staging_dir: "{{ backup_control_staging_root }}/{{ backup_id }}"
    wordpress_backup_filename: >-
      wordpress-files-{{ inventory_hostname }}-{{ backup_id }}.tar.gz
    wordpress_backup_path: >-
      {{ backup_remote_work_dir }}/{{ wordpress_backup_filename }}

  tasks:
    - name: WordPress 諛깆뾽 ?꾧뎄 ?ㅼ튂
      ansible.builtin.apt:
        name:
          - gzip
        state: present
        update_cache: true
        cache_valid_time: 3600

    - name: Web ?몃뱶 ?꾩떆 諛깆뾽 ?붾젆?곕━ ?앹꽦
      ansible.builtin.file:
        path: "{{ backup_remote_work_dir }}"
        state: directory
        owner: root
        group: root
        mode: "0750"

    - name: WordPress 而⑦뀒?대꼫 ?뺣낫 ?뺤씤
      community.docker.docker_container_info:
        name: "{{ wordpress_container_name }}"
      register: wordpress_container_info

    - name: WordPress 而⑦뀒?대꼫 ?ㅽ뻾 ?곹깭 寃利?
      ansible.builtin.assert:
        that:
          - wordpress_container_info.exists
          - wordpress_container_info.container.State.Running | default(false) | bool
        fail_msg: >-
          {{ wordpress_container_name }} 而⑦뀒?대꼫媛 ?녾굅???ㅽ뻾 以묒씠 ?꾨떃?덈떎.
        success_msg: "WordPress 而⑦뀒?대꼫媛 ?ㅽ뻾 以묒엯?덈떎."

    - name: WordPress ?뚯씪 ?뺤텞 諛깆뾽 ?앹꽦
      ansible.builtin.shell:
        cmd: |
          set -o pipefail
          umask 077

          docker exec "{{ wordpress_container_name }}" \
            tar -cf - -C /var/www/html . \
            | gzip -9 > "{{ wordpress_backup_path }}"
        executable: /bin/bash
      changed_when: true

    - name: WordPress 諛깆뾽 ?뚯씪 ?뺣낫 ?뺤씤
      ansible.builtin.stat:
        path: "{{ wordpress_backup_path }}"
      register: wordpress_backup_stat

    - name: WordPress 諛깆뾽 ?뚯씪 ?앹꽦 寃곌낵 寃利?
      ansible.builtin.assert:
        that:
          - wordpress_backup_stat.stat.exists
          - wordpress_backup_stat.stat.isreg
          - wordpress_backup_stat.stat.size | int > 0
        fail_msg: "WordPress ?뚯씪 諛깆뾽???뺤긽?곸쑝濡??앹꽦?섏? ?딆븯?듬땲??"
        success_msg: "WordPress ?뚯씪 諛깆뾽???뺤긽?곸쑝濡??앹꽦?섏뿀?듬땲??"

    - name: WordPress 諛깆뾽 ?뚯씪 ?뚯쑀沅?蹂寃?
      ansible.builtin.file:
        path: "{{ wordpress_backup_path }}"
        owner: ubuntu
        group: ubuntu
        mode: "0600"

    - name: WordPress 諛깆뾽 ?뚯씪??Control ?몃뱶濡?媛?몄삤湲?
      ansible.builtin.fetch:
        src: "{{ wordpress_backup_path }}"
        dest: "{{ control_staging_dir }}/"
        flat: true
        validate_checksum: true
      become: false

    - name: Web ?몃뱶 ?꾩떆 諛깆뾽 ?뚯씪 ?쒓굅
      ansible.builtin.file:
        path: "{{ wordpress_backup_path }}"
        state: absent


- name: Backup ?몃뱶??諛깆뾽 ?뚯씪 ???
  hosts: backup
  become: true
  gather_facts: true

  vars:
    backup_id: "{{ hostvars['localhost'].dandelion_backup_id }}"
    control_staging_dir: "{{ backup_control_staging_root }}/{{ backup_id }}"
    backup_destination_dir: "{{ backup_storage_dir }}/{{ backup_id }}"

  tasks:
    - name: Backup ?몃뱶 湲곕낯 ????붾젆?곕━ ?앹꽦
      ansible.builtin.file:
        path: "{{ backup_storage_dir }}"
        state: directory
        owner: root
        group: root
        mode: "0750"

    - name: ?꾩옱 ?ㅽ뻾 諛깆뾽 ?붾젆?곕━ ?앹꽦
      ansible.builtin.file:
        path: "{{ backup_destination_dir }}"
        state: directory
        owner: root
        group: root
        mode: "0750"

    - name: MariaDB 諛깆뾽 ?뚯씪??Backup ?몃뱶?????
      ansible.builtin.copy:
        src: >-
          {{ control_staging_dir }}/wordpress-db-{{ item }}-{{ backup_id }}.sql.gz
        dest: >-
          {{ backup_destination_dir }}/wordpress-db-{{ item }}-{{ backup_id }}.sql.gz
        owner: root
        group: root
        mode: "0600"
      loop: "{{ groups['db'] }}"

    - name: WordPress 諛깆뾽 ?뚯씪??Backup ?몃뱶?????
      ansible.builtin.copy:
        src: >-
          {{ control_staging_dir }}/wordpress-files-{{ item }}-{{ backup_id }}.tar.gz
        dest: >-
          {{ backup_destination_dir }}/wordpress-files-{{ item }}-{{ backup_id }}.tar.gz
        owner: root
        group: root
        mode: "0600"
      loop: "{{ groups['web'] }}"

    - name: Backup ?몃뱶????λ맂 諛깆뾽 ?뚯씪 寃??
      ansible.builtin.find:
        paths: "{{ backup_destination_dir }}"
        file_type: file
        recurse: false
        patterns:
          - "*.sql.gz"
          - "*.tar.gz"
      register: stored_backup_files

    - name: 諛깆뾽 ?뚯씪 媛쒖닔 寃利?
      ansible.builtin.assert:
        that:
          - >-
            stored_backup_files.matched | int ==
            (groups['db'] | length) + (groups['web'] | length)
        fail_msg: "Backup ?몃뱶???꾩슂??諛깆뾽 ?뚯씪??紐⑤몢 ??λ릺吏 ?딆븯?듬땲??"
        success_msg: "DB 諛?Web ?몃뱶 諛깆뾽 ?뚯씪??紐⑤몢 ??λ릺?덉뒿?덈떎."

    - name: 諛깆뾽 ?뚯씪 SHA256 泥댄겕??怨꾩궛
      ansible.builtin.command:
        argv:
          - sha256sum
          - "{{ item.path | basename }}"
        chdir: "{{ backup_destination_dir }}"
      loop: "{{ stored_backup_files.files }}"
      register: backup_checksum_results
      changed_when: false
      loop_control:
        label: "{{ item.path | basename }}"

    - name: SHA256 泥댄겕???뚯씪 ?앹꽦
      ansible.builtin.copy:
        dest: "{{ backup_destination_dir }}/SHA256SUMS"
        owner: root
        group: root
        mode: "0600"
        content: |
          # Dandelion Backup
          # Backup ID: {{ backup_id }}
          # Created: {{ ansible_date_time.iso8601 }}

          {% for result in backup_checksum_results.results %}
          {{ result.stdout }}
          {% endfor %}

    - name: 蹂닿? 湲곌컙??吏??諛깆뾽 ?붾젆?곕━ 寃??
      ansible.builtin.find:
        paths: "{{ backup_storage_dir }}"
        file_type: directory
        recurse: false
        patterns:
          - "20*"
        age: "{{ backup_retention_days }}d"
      register: expired_backup_directories

    - name: 蹂닿? 湲곌컙??吏??諛깆뾽 ?붾젆?곕━ ?쒓굅
      ansible.builtin.file:
        path: "{{ item.path }}"
        state: absent
      loop: "{{ expired_backup_directories.files }}"
      loop_control:
        label: "{{ item.path | basename }}"

    - name: 諛깆뾽 ?꾨즺 ?곹깭 湲곕줉
      ansible.builtin.set_fact:
        dandelion_backup_complete: true

    - name: 諛깆뾽 寃곌낵 異쒕젰
      ansible.builtin.debug:
        msg:
          - "諛깆뾽 ID: {{ backup_id }}"
          - "????꾩튂: {{ backup_destination_dir }}"
          - "諛깆뾽 ?뚯씪 ?? {{ stored_backup_files.matched }}"
          - "蹂닿? 湲곌컙: {{ backup_retention_days }}??


- name: Control ?몃뱶 ?꾩떆 諛깆뾽 ?뚯씪 ?뺣━
  hosts: localhost
  gather_facts: false
  become: false

  vars:
    backup_id: "{{ hostvars['localhost'].dandelion_backup_id }}"
    control_staging_dir: "{{ backup_control_staging_root }}/{{ backup_id }}"

  tasks:
    - name: 諛깆뾽 ?꾨떖 ?꾨즺 ???꾩떆 ?붾젆?곕━ ?쒓굅
      ansible.builtin.file:
        path: "{{ control_staging_dir }}"
        state: absent
      when:
        - groups['backup'] | length > 0
        - >-
          hostvars[groups['backup'][0]].dandelion_backup_complete
          | default(false)
          | bool
```

---

# 11. backup.yml 二쇱슂 肄붾뱶 ?ㅻ챸

## 11.1 諛깆뾽 ?ㅽ뻾 ID

```yaml
dandelion_backup_id: "{{ lookup('pipe', 'date +%Y%m%d_%H%M%S') }}"
```

Playbook ?ㅽ뻾 ?쒓컖??湲곗??쇰줈 怨좎쑀 諛깆뾽 ID瑜??앹꽦?쒕떎.

?덉떆:

```text
20260624_153000
```

紐⑤뱺 ?몃뱶??諛깆뾽 ?뚯씪??媛숈? ID瑜??ъ슜?섎?濡???踰덉쓽 諛깆뾽 ?ㅽ뻾 寃곌낵瑜?援щ텇?????덈떎.

## 11.2 MariaDB ?ㅽ봽 ?앹꽦

```bash
mariadb-dump
```

二쇱슂 ?듭뀡:

| ?듭뀡 | ??븷 |
|---|---|
| `--single-transaction` | InnoDB ?뚯씠釉붿쓣 ?μ떆媛??좉렇吏 ?딄퀬 ?쇨????ㅽ봽 ?앹꽦 |
| `--quick` | ?뚯씠釉??곗씠?곕? ??踰덉뿉 硫붾え由ъ뿉 ?щ━吏 ?딄퀬 ?쒖감 泥섎━ |
| `--skip-lock-tables` | 紐낆떆?곸씤 ?뚯씠釉??좉툑 諛⑹? |
| `--routines` | Stored Procedure? Function ?ы븿 |
| `--triggers` | Trigger ?ы븿 |
| `--events` | Event Scheduler ??ぉ ?ы븿 |
| `--default-character-set=utf8mb4` | WordPress 臾몄옄??吏??|

?앹꽦 寃곌낵:

```text
wordpress-db-db-node-諛깆뾽ID.sql.gz
```

## 11.3 WordPress ?뚯씪 諛깆뾽

癒쇱? WordPress 而⑦뀒?대꼫媛 議댁옱?섍퀬 ?ㅽ뻾 以묒씤吏 ?뺤씤?쒕떎.

```yaml
community.docker.docker_container_info:
  name: "{{ wordpress_container_name }}"
```

?댄썑 而⑦뀒?대꼫 ?대???`/var/www/html`???뺤텞?쒕떎.

```bash
docker exec dandelion-wordpress \
  tar -cf - -C /var/www/html . \
  | gzip -9 > wordpress-files.tar.gz
```

Web ?몃뱶蹂??앹꽦 寃곌낵:

```text
wordpress-files-web1-node-諛깆뾽ID.tar.gz
wordpress-files-web2-node-諛깆뾽ID.tar.gz
```

## 11.4 fetch? copy瑜??댁슜???꾨떖

```yaml
ansible.builtin.fetch
```

DB 諛?Web ?몃뱶??諛깆뾽 ?뚯씪??Control ?몃뱶濡?媛?몄삩??

```yaml
ansible.builtin.copy
```

Control ?몃뱶???꾩떆 ?뚯씪??Backup ?몃뱶濡??꾩넚?쒕떎.

## 11.5 諛깆뾽 ?뚯씪 媛쒖닔 寃利?

?꾩옱 DB ?몃뱶 1?? Web ?몃뱶 2?瑜?湲곗??쇰줈 珥???媛쒖쓽 諛깆뾽 ?뚯씪???앹꽦?섏뼱???쒕떎.

```text
DB 諛깆뾽 ?뚯씪 1媛?
WordPress ?뚯씪 諛깆뾽 2媛?
珥?3媛?
```

?덉긽 媛쒖닔? ?ㅼ젣 媛쒖닔媛 ?ㅻⅤ硫?Playbook???ㅽ뙣 泥섎━?쒕떎.

## 11.6 SHA256 泥댄겕??

媛?諛깆뾽 ?뚯씪??SHA256 媛믪쓣 怨꾩궛?섏뿬 ?ㅼ쓬 ?뚯씪????ν븳??

```text
SHA256SUMS
```

諛깆뾽 ?뚯씪???꾩넚 ?먮뒗 蹂닿? 怨쇱젙?먯꽌 ?먯긽?섏? ?딆븯?붿? 寃利앺븷 ???ъ슜?쒕떎.

## 11.7 ?ㅻ옒??諛깆뾽 ?쒓굅

```yaml
backup_retention_days: 7
```

?꾩옱 ?ㅼ젙 湲곗??쇰줈 7?쇱씠 吏??諛깆뾽 ?붾젆?곕━瑜?寃?됲븯???쒓굅?쒕떎.

## 11.8 ?꾩떆 ?뚯씪 ?뺣━

Backup ?몃뱶 ??κ낵 寃利앹씠 紐⑤몢 ?깃났??寃쎌슦?먮쭔 Control ?몃뱶???꾩떆 諛깆뾽 ?붾젆?곕━瑜??쒓굅?쒕떎.

諛깆뾽 ?ㅽ뙣 ?쒖뿉??臾몄젣 ?뺤씤???꾪빐 ?꾩떆 ?뚯씪???⑥쓣 ???덈떎.

---

# 12. Backup ?몃뱶 ?덉긽 ???援ъ“

```text
/srv/dandelion-backups/
?붴?? 20260624_153000/
    ?쒋?? wordpress-db-db-node-20260624_153000.sql.gz
    ?쒋?? wordpress-files-web1-node-20260624_153000.tar.gz
    ?쒋?? wordpress-files-web2-node-20260624_153000.tar.gz
    ?붴?? SHA256SUMS
```

---

# 13. backup.yml 寃??紐낅졊

臾몃쾿 寃??

```bash
cd ~/dandeliondir/ansible
ansible-playbook --syntax-check playbooks/backup.yml
```

?ㅽ뻾 ????뺤씤:

```bash
ansible-playbook playbooks/backup.yml --list-hosts
```

?덉긽 ???

```text
localhost
db-node
web1-node
web2-node
backup-node
```

?ㅼ젣 ?ㅽ뻾:

```bash
ansible-playbook playbooks/backup.yml
```

?꾩옱 ?ㅼ젣 ?ㅽ뻾? 蹂대쪟 ?곹깭?대떎.

---

# 14. 諛깆뾽 ?ㅽ뻾 ???뺤씤 紐낅졊

Backup ?몃뱶????λ맂 ?뚯씪 ?뺤씤:

```bash
sudo find /srv/dandelion-backups -maxdepth 2 -type f -ls
```

諛깆뾽 ?붾젆?곕━ 紐⑸줉 ?뺤씤:

```bash
sudo ls -lh /srv/dandelion-backups/
```

泥댄겕??寃利?

```bash
cd /srv/dandelion-backups/諛깆뾽ID
sudo sha256sum -c SHA256SUMS
```

?뺤긽 寃곌낵 ?덉떆:

```text
wordpress-db-db-node-20260624_153000.sql.gz: OK
wordpress-files-web1-node-20260624_153000.tar.gz: OK
wordpress-files-web2-node-20260624_153000.tar.gz: OK
```

---

# 15. ?듭떖 Playbook ?묒꽦 ?곹깭

?꾩옱 `site.yml`???ы븿?섎뒗 ?듭떖 Playbook? 紐⑤몢 ?묒꽦???곹깭?대떎.

```text
playbooks/
?쒋?? common.yml       # 怨듯넻 ?⑦궎吏 諛?湲곕낯 ?섍꼍 援ъ꽦
?쒋?? docker.yml       # Web ?몃뱶 Docker ?ㅼ튂
?쒋?? database.yml     # DB ?몃뱶 MariaDB 援ъ꽦
?쒋?? wordpress.yml    # Custom WordPress 諛고룷
?쒋?? proxy.yml        # HAProxy 濡쒕뱶諛몃윴??援ъ꽦
?붴?? backup.yml       # DB 諛?WordPress ?뚯씪 諛깆뾽
```

沅뚯옣 `site.yml` ?ㅽ뻾 ?쒖꽌:

```yaml
---
- name: 怨듯넻 ?쒕쾭 ?섍꼍 援ъ꽦
  import_playbook: playbooks/common.yml

- name: Web ?몃뱶 Docker ?섍꼍 援ъ꽦
  import_playbook: playbooks/docker.yml

- name: DB ?몃뱶 MariaDB ?섍꼍 援ъ꽦
  import_playbook: playbooks/database.yml

- name: Web ?몃뱶 WordPress 諛고룷
  import_playbook: playbooks/wordpress.yml

- name: Proxy ?몃뱶 HAProxy 援ъ꽦
  import_playbook: playbooks/proxy.yml

- name: 諛깆뾽 ?섍꼍 援ъ꽦 諛?諛깆뾽 ?ㅽ뻾
  import_playbook: playbooks/backup.yml
```

---

# 16. ?꾩옱 ?붾젆?곕━ 援ъ“

```text
dandeliondir/
?붴?? ansible/
    ?쒋?? ansible.cfg
    ?쒋?? inventory.ini
    ?쒋?? requirements.yml
    ?쒋?? site.yml
    ?쒋?? group_vars/
    ??  ?붴?? all.yml
    ?쒋?? files/
    ??  ?붴?? wordpress/
    ??      ?쒋?? Dockerfile
    ??      ?붴?? custom.ini
    ?쒋?? templates/
    ??  ?쒋?? wordpress-compose.yml.j2
    ??  ?붴?? haproxy.cfg.j2
    ?붴?? playbooks/
        ?쒋?? common.yml
        ?쒋?? docker.yml
        ?쒋?? database.yml
        ?쒋?? wordpress.yml
        ?쒋?? proxy.yml
        ?붴?? backup.yml
```

---

# 17. ?꾩옱 ?묒뾽 ?곹깭

| ?묒뾽 | ?곹깭 |
|---|---|
| HAProxy 怨듯넻 蹂??異붽? | ?꾨즺 |
| `haproxy.cfg.j2` ?묒꽦 | ?꾨즺 |
| `proxy.yml` ?묒꽦 | ?꾨즺 |
| `proxy.yml` 肄붾뱶 ?ㅻ챸 | ?꾨즺 |
| 諛깆뾽 怨듯넻 蹂??異붽? | ?꾨즺 |
| 諛깆뾽 ?꾩넚 援ъ“ ?ㅺ퀎 | ?꾨즺 |
| `backup.yml` ?묒꽦 | ?꾨즺 |
| ?듭떖 Playbook 肄붾뱶 ?묒꽦 | ?꾨즺 |
| ?꾩껜 臾몃쾿 寃??| 誘몄떎??|
| ?ㅼ젣 ?몃뱶 ?곸슜 | 誘몄떎??|
| 硫깅벑??寃利?| 誘몄떎??|
| Proxy 寃쎌쑀 ?묒냽 寃利?| 誘몄떎??|
| ?ㅼ젣 諛깆뾽 ?앹꽦 寃利?| 誘몄떎??|
| 蹂듦뎄 ?덉감 寃利?| 誘몄떎??|

---

# 18. ?ㅼ젣 ?ㅽ뻾 ???뺤씤?ы빆

## 18.1 Collection ?ㅼ튂 ?뺤씤

```bash
cd ~/dandeliondir/ansible
ansible-galaxy collection install -r requirements.yml
ansible-galaxy collection list
```

?꾩슂 Collection:

```text
ansible.mysql
community.docker
```

## 18.2 Web ?몃뱶 WordPress ?곹깭

`proxy.yml`怨?`backup.yml`? WordPress 而⑦뀒?대꼫媛 ?ㅽ뻾 以묒씤 ?곹깭瑜??꾩젣濡??쒕떎.

```bash
ansible web -b -m command -a "docker ps"
```

## 18.3 DB ?몃뱶 MariaDB ?곹깭

```bash
ansible db -b -m command -a "systemctl is-active mariadb"
```

## 18.4 蹂댁븞洹몃９

?꾩슂???듭떊:

```text
Control Node ???꾩껜 愿由??몃뱶 TCP 22
Proxy Node ??Web Node TCP 80
Web Node ??DB Node TCP 3306
```

DB??TCP 3306 ?ы듃???명꽣???꾩껜??怨듦컻?섏? ?딄퀬 Web ?몃뱶?먯꽌留??묎렐?섎룄濡??쒗븳?쒕떎.

## 18.5 Proxy ?몃뱶 80踰??ы듃

湲곗〈 Apache, Nginx ?먮뒗 ?ㅻⅨ ?꾨줈?몄뒪媛 ?ъ슜 以묒씠吏 ?딆?吏 ?뺤씤?쒕떎.

```bash
ansible proxy -b -m shell -a "ss -lntp | grep ':80' || true"
```

---

# 19. 沅뚯옣 寃??諛??ㅽ뻾 ?쒖꽌

## 19.1 ?꾩껜 臾몃쾿 寃??

```bash
cd ~/dandeliondir/ansible

ansible-playbook --syntax-check playbooks/common.yml
ansible-playbook --syntax-check playbooks/docker.yml
ansible-playbook --syntax-check playbooks/database.yml
ansible-playbook --syntax-check playbooks/wordpress.yml
ansible-playbook --syntax-check playbooks/proxy.yml
ansible-playbook --syntax-check playbooks/backup.yml
ansible-playbook --syntax-check site.yml
```

## 19.2 ????몃뱶 ?뺤씤

```bash
ansible-playbook site.yml --list-hosts
```

## 19.3 ?ㅼ젣 ?ㅽ뻾 ?쒖꽌

??踰덉뿉 `site.yml` ?꾩껜瑜??ㅽ뻾?섍린 ?꾩뿉 媛쒕퀎 Playbook???쒖꽌?濡??곸슜?섎뒗 寃껋쓣 沅뚯옣?쒕떎.

```bash
ansible-playbook playbooks/common.yml
ansible-playbook playbooks/docker.yml
ansible-playbook playbooks/database.yml
ansible-playbook playbooks/wordpress.yml
ansible-playbook playbooks/proxy.yml
ansible-playbook playbooks/backup.yml
```

媛??ㅽ뻾 ???ㅼ쓬 寃곌낵瑜??뺤씤?쒕떎.

```text
unreachable=0
failed=0
```

`backup.yml`? ?ㅽ뻾???뚮쭏???덈줈??諛깆뾽???앹꽦?섎뒗 ?묒뾽?대?濡??쇰컲?곸씤 `changed=0` 硫깅벑??寃利???곸뿉?쒕뒗 ?쒖쇅?쒕떎.

---

# 20. ?ㅼ쓬 ?묒뾽

?듭떖 Playbook ?묒꽦? ?꾨즺?섏뿀?쇰ŉ ?ㅼ쓬 ?④퀎遺?곕뒗 ?ㅽ뻾怨?寃利??묒뾽??吏꾪뻾?쒕떎.

```text
1. 紐⑤뱺 Playbook 臾몃쾿 寃??
2. 媛?Playbook ?ㅽ뻾 ????뺤씤
3. common.yml遺???쒖꽌?濡??ㅼ젣 ?곸슜
4. ?ㅻ쪟 諛쒖깮 ???섏젙 諛??ъ떎??
5. ?쇰컲 援ъ꽦 Playbook??硫깅벑??寃利?
6. Proxy 寃쎌쑀 WordPress ?묒냽 ?뺤씤
7. 諛깆뾽 ?뚯씪 ?앹꽦 諛?泥댄겕???뺤씤
8. health_check.sh ?묒꽦
9. restore.md 蹂듦뎄 ?덉감 ?묒꽦
10. MariaDB 諛?WordPress ?뚯씪 蹂듦뎄 寃利?
11. ?ㅽ뻾 寃곌낵 諛??몃윭釉붿뒋??罹≪쿂
```

?꾩옱 ?곹깭瑜??뺣━?섎㈃ ?ㅼ쓬怨?媛숇떎.

```text
?듭떖 Playbook 肄붾뱶 ?묒꽦 ?꾨즺
?ㅼ젣 ?ㅽ뻾 諛??쒕퉬??寃利?誘몄셿猷?
諛깆뾽 蹂듦뎄 寃利?臾몄꽌 誘몄옉??
```

