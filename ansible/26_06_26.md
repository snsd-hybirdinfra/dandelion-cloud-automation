> Note: 본 문서는 작성 당시 진행 상황을 기준으로 작성된 작업 기록이다.
> 최신 아키텍처 기준은 `docs/current-status.md` 및 `docs/architecture.md`를 따른다.

---
<!-- STATUS: COMPLETE -->
# 2026-06-26 HAProxy Playbook ?곸슜 ?뺣━

## 1. 臾몄꽌 踰붿쐞

蹂?臾몄꽌??`26_06_25.md` ?댄썑 吏꾪뻾???묒뾽???뺣━?쒕떎.

?대쾲 ?묒뾽 踰붿쐞???ㅼ쓬怨?媛숇떎.

```text
proxy.yml ?ㅽ뻾 ??湲곗〈 HAProxy ?ㅼ젙 ?뺤씤
??湲곗〈 HTTPS ?ㅼ젙怨?Playbook ?쒗뵆由?鍮꾧탳
??HTTPS ?ㅼ젙??Ansible 蹂??諛??쒗뵆由우뿉 諛섏쁺
??Check Mode ?ㅻ쪟 ?섏젙
??proxy.yml ?ㅼ젣 ?ㅽ뻾
??HAProxy ?쒕퉬??諛?HTTP/HTTPS ?숈옉 ?뺤씤
```

`backup.yml`? ?꾩쭅 ?쒖옉?섏? ?딆븯?쇰ŉ ??臾몄꽌???ы븿?섏? ?딅뒗??

---

# 2. proxy.yml 援ъ꽦 ?뺤씤

?뺤씤??Playbook:

```text
playbooks/proxy.yml
```

二쇱슂 泥섎━ ?쒖꽌:

```text
Inventory??web 洹몃９ ?뺤씤
??HAProxy ?⑦궎吏 ?ㅼ튂 ?곹깭 蹂댁옣
??Proxy ?몃뱶?먯꽌 Web1쨌Web2 HTTP ?묐떟 ?뺤씤
??HAProxy ?ㅼ젙 ?쒗뵆由?諛고룷
???ㅼ젙 蹂寃???HAProxy ?ъ떆??
??HAProxy ?쒕퉬???쒖꽦??諛??쒖옉
??Frontend ?ы듃 ?묐떟 ?湲?
???ㅼ젙 臾몃쾿 ?뺤씤
??HAProxy 寃쎌쑀 WordPress ?묐떟 ?뺤씤
??HAProxy 踰꾩쟾 諛?援ъ꽦 寃곌낵 異쒕젰
```

湲곗〈 Playbook??Backend HTTP ?먭?? ?ㅼ쓬 ?곹깭 肄붾뱶瑜??뺤긽?쇰줈 泥섎━?쒕떎.

```text
200
301
302
```

?꾩옱 WordPress媛 珥덇린 ?ㅼ튂 ?붾㈃?쇰줈 ?대룞?섎뒗 ?곹깭??Web1怨?Web2 紐⑤몢 `302` ?묐떟???뺤긽?대떎.

---

# 3. 湲곗〈 HAProxy ?섍꼍 ?뺤씤

## 3.1 HAProxy ?ㅼ튂 諛??쒕퉬???곹깭

```bash
ansible proxy-node -b -m shell -a \
"haproxy -v | head -n 1; systemctl is-active haproxy; systemctl is-enabled haproxy"
```

?뺤씤 寃곌낵:

```text
HAProxy version 2.8.16-0ubuntu0.24.04.3
active
enabled
```

## 3.2 湲곗〈 ?ㅼ젙 臾몃쾿 ?뺤씤

```bash
ansible proxy-node -b -m command -a \
"haproxy -c -f /etc/haproxy/haproxy.cfg"
```

寃곌낵:

```text
Configuration file is valid
```

## 3.3 湲곗〈 ?섎룞 ?ㅼ젙 ?댁슜

湲곗〈 `/etc/haproxy/haproxy.cfg`?먮뒗 ?ㅼ쓬 援ъ꽦??議댁옱?덈떎.

```text
HTTP 80 ?ы듃 ?섏떊
HTTPS 443 ?ы듃 ?섏떊
SSL ?몄쬆???ъ슜
HTTP ??HTTPS 由щ떎?대젆??
X-Forwarded-Proto ?ㅻ뜑 ?ㅼ젙
X-Forwarded-Port ?ㅻ뜑 ?ㅼ젙
Web1쨌Web2 Round Robin 濡쒕뱶諛몃윴??
```

二쇱슂 湲곗〈 ?ㅼ젙:

```haproxy
frontend proxy_front
    bind *:80
    bind *:443 ssl crt /etc/haproxy/certs/dandelion-wp.pem
    redirect scheme https if !{ ssl_fc }
    default_backend web_backend

backend web_backend
    balance roundrobin
    http-request set-header X-Forwarded-Proto https if { ssl_fc }
    http-request set-header X-Forwarded-Port 443 if { ssl_fc }
    server web1 192.168.4.4:80 check
    server web2 192.168.4.35:80 check
```

## 3.4 HAProxy ?ы듃 ?뺤씤

```bash
ansible proxy-node -b -m shell -a \
"ss -lntp '( sport = :80 )'"
```

寃곌낵:

```text
0.0.0.0:80 ??haproxy
```

HAProxy媛 80踰??ы듃瑜??뺤긽?곸쑝濡??ъ슜?섍퀬 ?덉뿀??

## 3.5 Proxy?먯꽌 Web ?몃뱶 吏곸젒 ?묐떟 ?뺤씤

```bash
ansible proxy-node -m shell -a \
"for ip in 192.168.4.4 192.168.4.35; do echo WEB=\$ip; curl -sS -o /dev/null -w '%{http_code}\n' http://\$ip; done"
```

寃곌낵:

```text
192.168.4.4  ??302
192.168.4.35 ??302
```

??WordPress Web ?몃뱶 紐⑤몢 Proxy?먯꽌 ?뺤긽 ?묎렐 媛?ν뻽??

---

# 4. 湲곗〈 ?ㅼ젙怨?Ansible ?쒗뵆由?鍮꾧탳

珥덇린 `templates/haproxy.cfg.j2`?먮뒗 HTTP 80 ?ы듃? Web Backend 濡쒕뱶諛몃윴?깅쭔 ?ы븿?섏뼱 ?덉뿀??

珥덇린 ?쒗뵆由우뿉 ?녿뜕 湲곗〈 湲곕뒫:

```text
HTTPS 443 ?ы듃
SSL ?몄쬆??
HTTP ??HTTPS 由щ떎?대젆??
X-Forwarded-Proto
X-Forwarded-Port
SSL 湲곕낯 ?뷀샇???ㅼ젙
```

珥덇린 ?쒗뵆由우쓣 洹몃?濡??곸슜?섎㈃ 湲곗〈 HTTPS 援ъ꽦???щ씪吏????덉쑝誘濡??ㅼ젣 ?ㅽ뻾??以묐떒?섍퀬 ?쒗뵆由우쓣 癒쇱? ?섏젙?섏???

---

# 5. HAProxy 蹂??異붽?

`group_vars/all.yml`??HTTPS 愿??蹂?섎? 異붽??섏???

```yaml
haproxy_frontend_port: 80
haproxy_https_port: 443
haproxy_backend_port: 80
haproxy_balance_algorithm: roundrobin
haproxy_ssl_cert_path: "/etc/haproxy/certs/dandelion-wp.pem"
```

蹂???섎?:

| 蹂??| 媛?| ?⑸룄 |
|---|---:|---|
| `haproxy_frontend_port` | `80` | HTTP Frontend ?ы듃 |
| `haproxy_https_port` | `443` | HTTPS Frontend ?ы듃 |
| `haproxy_backend_port` | `80` | WordPress Web Backend ?ы듃 |
| `haproxy_balance_algorithm` | `roundrobin` | 濡쒕뱶諛몃윴??諛⑹떇 |
| `haproxy_ssl_cert_path` | ?몄쬆??寃쎈줈 | HAProxy SSL ?몄쬆??|

?몄쬆???뚯씪??Proxy ?몃뱶??議댁옱?섎뒗吏 ?뺤씤?섏???

```text
/etc/haproxy/certs/dandelion-wp.pem
```

---

# 6. haproxy.cfg.j2 ?섏젙

?섏젙???쒗뵆由?

```text
templates/haproxy.cfg.j2
```

理쒖쥌 援ъ“:

```haproxy
# Ansible?먯꽌 ?먮룞 ?앹꽦??HAProxy ?ㅼ젙 ?뚯씪

global
    log /dev/log local0
    log /dev/log local1 notice
    chroot /var/lib/haproxy
    stats socket /run/haproxy/admin.sock mode 660 level admin
    stats timeout 30s
    user haproxy
    group haproxy
    daemon

    ca-base /etc/ssl/certs
    crt-base /etc/ssl/private

    ssl-default-bind-ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384
    ssl-default-bind-ciphersuites TLS_AES_128_GCM_SHA256:TLS_AES_256_GCM_SHA384:TLS_CHACHA20_POLY1305_SHA256
    ssl-default-bind-options ssl-min-ver TLSv1.2 no-tls-tickets

defaults
    log global
    mode http
    option httplog
    option dontlognull
    option forwardfor
    option http-server-close

    timeout connect 5s
    timeout client 50s
    timeout server 50s
    timeout http-request 10s
    timeout http-keep-alive 10s

    errorfile 400 /etc/haproxy/errors/400.http
    errorfile 403 /etc/haproxy/errors/403.http
    errorfile 408 /etc/haproxy/errors/408.http
    errorfile 500 /etc/haproxy/errors/500.http
    errorfile 502 /etc/haproxy/errors/502.http
    errorfile 503 /etc/haproxy/errors/503.http
    errorfile 504 /etc/haproxy/errors/504.http

frontend wordpress_frontend
    bind *:{{ haproxy_frontend_port }}
    bind *:{{ haproxy_https_port }} ssl crt {{ haproxy_ssl_cert_path }}

    redirect scheme https code 301 if !{ ssl_fc }

    http-request set-header X-Forwarded-Proto https if { ssl_fc }
    http-request set-header X-Forwarded-Port {{ haproxy_https_port }} if { ssl_fc }

    default_backend wordpress_backend

backend wordpress_backend
    balance {{ haproxy_balance_algorithm }}
    option httpchk GET /

{% for host in groups['web'] %}
    server {{ host }} {{ hostvars[host].ansible_host }}:{{ haproxy_backend_port }} check inter 3s fall 3 rise 2
{% endfor %}
```

?쒗뵆由??곸슜 ??Inventory??`web` 洹몃９??湲곗??쇰줈 ?ㅼ쓬 Backend媛 ?먮룞 ?앹꽦?쒕떎.

```text
web1-node 192.168.4.4:80
web2-node 192.168.4.35:80
```

---

# 7. ?쒗뵆由??뚮뜑留??뺤씤

?ㅼ젣 ?ㅼ젙 ?뚯씪??蹂寃쏀븯湲??꾩뿉 ?쒗뵆由??뚮뜑留?寃곌낵瑜??뺤씤?섏???

```bash
ansible proxy-node -m template \
-a "src=templates/haproxy.cfg.j2 dest=/tmp/haproxy.cfg.preview mode=0644" \
--check --diff
```

?뚮뜑留?寃곌낵?먯꽌 ?ㅼ쓬 ??ぉ???뺤긽?곸쑝濡??앹꽦?섏뿀??

```text
bind *:80
bind *:443 ssl crt /etc/haproxy/certs/dandelion-wp.pem
HTTP ??HTTPS 301 由щ떎?대젆??
X-Forwarded-Proto https
X-Forwarded-Port 443
Round Robin
Web1쨌Web2 Backend
```

---

# 8. proxy.yml Check Mode 泥?踰덉㎏ ?ㅻ쪟

?ㅽ뻾:

```bash
ansible-playbook playbooks/proxy.yml --check --diff
```

泥?踰덉㎏ ?ㅻ쪟:

```text
'dict object' has no attribute 'status'
```

?ㅻ쪟媛 諛쒖깮??議곌굔:

```yaml
until: web_backend_check.status in [200, 301, 302]
```

## 8.1 ?먯씤

Check Mode?먯꽌 `ansible.builtin.uri`媛 ?ㅼ젣 HTTP ?붿껌???섑뻾?섏? ?딆븘 ?깅줉 蹂?섏뿉 `status`媛 ?놁뿀??

## 8.2 ?섏젙

HTTP ?곹깭 ?뺤씤 Task???ㅼ쓬 Task ?듭뀡??異붽??섏???

```yaml
check_mode: false
changed_when: false
```

?곸슜 ???

```text
Proxy ?몃뱶?먯꽌 Web ?몃뱶 HTTP ?묐떟 ?뺤씤
HAProxy 寃쎌쑀 WordPress HTTP ?묐떟 ?뺤씤
```

`check_mode: false`???꾩껜 Playbook??`--check`濡??ㅽ뻾?섎뜑?쇰룄 ?대떦 議고쉶 Task???ㅼ젣濡??섑뻾?섎룄濡??쒕떎.

---

# 9. proxy.yml Check Mode ??踰덉㎏ ?ㅻ쪟

??踰덉㎏ Check Mode?먯꽌??留덉?留?Debug Task?먯꽌 ?ㅻ쪟媛 諛쒖깮?섏???

```text
list object has no element 0
```

臾몄젣媛 ??媛?

```yaml
haproxy_version_result.stdout_lines[0]
```

## 9.1 ?먯씤

Check Mode?먯꽌 ?ㅼ쓬 議고쉶 Task媛 ?앸왂?섏뼱 ?깅줉 蹂?섏뿉 ?ㅼ젣 異쒕젰媛믪씠 ?놁뿀??

```text
HAProxy ?ы듃 ?묐떟 ?湲?
HAProxy ?ㅼ젙 臾몃쾿 ?뺤씤
HAProxy 踰꾩쟾 ?뺤씤
```

## 9.2 ?섏젙

?ㅼ쓬 議고쉶 諛?寃利?Task??`check_mode: false`瑜?異붽??섏???

```yaml
- name: HAProxy ?ы듃 ?묐떟 ?湲?
  ansible.builtin.wait_for:
    host: 127.0.0.1
    port: "{{ haproxy_frontend_port }}"
    timeout: 60
  check_mode: false
  changed_when: false

- name: HAProxy ?ㅼ젙 臾몃쾿 ?뺤씤
  ansible.builtin.command:
    cmd: haproxy -c -f /etc/haproxy/haproxy.cfg
  register: haproxy_config_check
  changed_when: false
  check_mode: false

- name: HAProxy 踰꾩쟾 ?뺤씤
  ansible.builtin.command:
    cmd: haproxy -v
  register: haproxy_version_result
  changed_when: false
  check_mode: false
```

---

# 10. check_mode ?ㅼ뿬?곌린 ?ㅻ쪟

?섏젙 怨쇱젙?먯꽌 `check_mode: false`媛 `wait_for` 紐⑤뱢 ?대????ㅼ뼱媛 ?ㅼ쓬 ?ㅻ쪟媛 諛쒖깮?섏???

```text
Unsupported parameters for (ansible.builtin.wait_for) module: check_mode
```

?섎せ???뺥깭:

```yaml
ansible.builtin.wait_for:
  host: 127.0.0.1
  port: 80
  timeout: 60
  check_mode: false
```

`check_mode`??紐⑤뱢 留ㅺ컻蹂?섍? ?꾨땲??Task ?듭뀡?대?濡??ㅼ쓬泥섎읆 紐⑤뱢怨?媛숈? ?섏???諛곗튂?댁빞 ?쒕떎.

```yaml
ansible.builtin.wait_for:
  host: 127.0.0.1
  port: 80
  timeout: 60
check_mode: false
changed_when: false
```

?ㅼ뿬?곌린瑜??섏젙????臾몃쾿 寃?ъ? Check Mode瑜??ㅼ떆 ?ㅽ뻾?섏???

---

# 11. 理쒖쥌 Check Mode 寃곌낵

?ㅽ뻾:

```bash
ansible-playbook --syntax-check playbooks/proxy.yml
ansible-playbook playbooks/proxy.yml --check --diff
```

理쒖쥌 寃곌낵:

```text
proxy-node:
ok=12
changed=2
unreachable=0
failed=0
skipped=0
```

援ъ꽦 寃곌낵 異쒕젰:

```text
HAProxy version 2.8.16-0ubuntu0.24.04.3
Configuration file is valid
Proxy ?몃뱶: proxy-node
?묒냽 二쇱냼: http://192.168.4.32:80
HTTP ?곹깭 肄붾뱶: 302
```

Check Mode??`changed=2`???ㅼ젣 蹂寃쎌씠 ?꾨땲???ㅼ젣 ?ㅽ뻾 ???ㅼ쓬 蹂寃쎌씠 諛쒖깮???덉젙?꾩쓣 ?섎??쒕떎.

```text
HAProxy ?ㅼ젙 ?뚯씪 援먯껜
HAProxy ?쒕퉬???ъ떆??
```

---

# 12. 湲곗〈 ?ㅼ젙 諛깆뾽

?ㅼ젣 ?ㅽ뻾 ?꾩뿉 湲곗〈 ?ㅼ젙??蹂꾨룄 ?뚯씪濡?諛깆뾽?섏???

```bash
ansible proxy-node -b -m copy -a \
"src=/etc/haproxy/haproxy.cfg dest=/etc/haproxy/haproxy.cfg.before-ansible remote_src=true owner=root group=root mode=0644"
```

諛깆뾽 寃쎈줈:

```text
/etc/haproxy/haproxy.cfg.before-ansible
```

Playbook??Template Task?먮룄 ?ㅼ쓬 ?듭뀡???덉뼱 ?ㅼ젙 蹂寃????먮룞 諛깆뾽??異붽?濡??앹꽦?쒕떎.

```yaml
backup: true
```

---

# 13. proxy.yml ?ㅼ젣 ?ㅽ뻾

Check Mode媛 ?뺤긽 ?듦낵?????ㅼ젣 Playbook???ㅽ뻾?섏???

```bash
ansible-playbook playbooks/proxy.yml
```

?ㅼ젣 ?곸슜 ?댁슜:

```text
HAProxy ?⑦궎吏 ?ㅼ튂 ?곹깭 ?뺤씤
Web1쨌Web2 Backend ?묐떟 ?뺤씤
?섏젙??HTTPS ?ы븿 ?ㅼ젙 ?뚯씪 諛고룷
?ㅼ젙 臾몃쾿 ?ъ쟾 寃利?
HAProxy ?쒕퉬???ъ떆??
HAProxy ?쒕퉬??enabled 諛?started ?곹깭 蹂댁옣
80踰??ы듃 ?묐떟 ?湲?
HAProxy ?ㅼ젙 臾몃쾿 ?ы솗??
HAProxy 寃쎌쑀 WordPress HTTP ?묐떟 ?뺤씤
```

?ъ슜???뺤씤 寃곌낵 ?ㅼ젣 ?ㅽ뻾怨??ы썑 寃利앷퉴吏 紐⑤몢 ?꾨즺?섏뿀??

---

# 14. ?ㅽ뻾 ??寃利?

## 14.1 ?쒕퉬???곹깭

```bash
ansible proxy-node -b -m shell -a \
"systemctl is-active haproxy; systemctl is-enabled haproxy"
```

?뺤긽 湲곗?:

```text
active
enabled
```

## 14.2 ?ㅼ젙 臾몃쾿

```bash
ansible proxy-node -b -m command -a \
"haproxy -c -f /etc/haproxy/haproxy.cfg"
```

?뺤긽 湲곗?:

```text
Configuration file is valid
```

## 14.3 HTTP 諛?HTTPS ?ы듃

```bash
ansible proxy-node -b -m shell -a \
"ss -lntp | grep -E ':80 |:443 '"
```

?뺤긽 ?곹깭:

```text
HAProxy媛 80踰??ы듃 ?섏떊
HAProxy媛 443踰??ы듃 ?섏떊
```

## 14.4 HTTP ??HTTPS 由щ떎?대젆??

```bash
ansible proxy-node -m shell -a \
"curl -sSI http://127.0.0.1 | head -n 1"
```

?뺤긽 湲곗?:

```text
HTTP/1.1 301 Moved Permanently
```

## 14.5 HTTPS 寃쎌쑀 WordPress ?묐떟

```bash
ansible proxy-node -m shell -a \
"curl -ksS -o /dev/null -w '%{http_code}\n' https://127.0.0.1"
```

?뺤긽 踰붿쐞:

```text
200
301
302
```

---

# 15. 理쒖쥌 HAProxy 援ъ꽦

理쒖쥌 援ъ꽦:

```text
Proxy ?몃뱶: 192.168.4.32
HTTP Frontend: 80
HTTPS Frontend: 443
SSL ?몄쬆?? /etc/haproxy/certs/dandelion-wp.pem
HTTP ?붿껌: HTTPS濡?301 由щ떎?대젆??
濡쒕뱶諛몃윴??諛⑹떇: roundrobin
Backend Web1: 192.168.4.4:80
Backend Web2: 192.168.4.35:80
Health Check: GET /
?μ븷 ?먯젙: 3???ㅽ뙣
蹂듦뎄 ?먯젙: 2???깃났
Health Check 媛꾧꺽: 3珥?
```

HTTP ?ㅻ뜑 泥섎━:

```text
X-Forwarded-For: option forwardfor濡?異붽?
X-Forwarded-Proto: HTTPS ?붿껌 ??https ?ㅼ젙
X-Forwarded-Port: HTTPS ?붿껌 ??443 ?ㅼ젙
```

---

# 16. ?꾩옱 Playbook 吏꾪뻾 ?곹깭

| Playbook | ?곹깭 |
|---|---|
| `common.yml` | ?ㅽ뻾 ?꾨즺 |
| `docker.yml` | ?ㅽ뻾 ?꾨즺 |
| `database.yml` | ?ㅽ뻾 ?꾨즺 |
| `wordpress.yml` | Web1쨌Web2 ?ㅽ뻾 ?꾨즺 |
| `proxy.yml` | HTTPS ?ㅼ젙 ?좎? ???ㅽ뻾 諛?寃利??꾨즺 |
| `backup.yml` | 誘몄떎??|

?꾩옱 ?④퀎:

```text
怨듯넻 ?섍꼍 援ъ꽦 ?꾨즺
??Docker 援ъ꽦 ?꾨즺
??MariaDB 援ъ꽦 ?꾨즺
??WordPress Web1쨌Web2 諛고룷 ?꾨즺
??HAProxy HTTP/HTTPS 濡쒕뱶諛몃윴??援ъ꽦 ?꾨즺
???ㅼ쓬 ?④퀎: backup.yml ?ㅽ뻾 ???먭?
```

