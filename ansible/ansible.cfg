# TEMP: 기본 예시 파일입니다. 실제 환경 값으로 교체하고 실행 결과 확인 후 TEMP 표시를 제거하세요.
[defaults]
inventory = ./inventory.ini
host_key_checking = False
retry_files_enabled = False
stdout_callback = yaml

[privilege_escalation]
become = True
become_method = sudo
become_user = root

