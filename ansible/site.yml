# TEMPLATE: 기본틀 파일입니다. 실제 환경 값 반영 및 실행 검증 후 이 표시를 제거하세요.
# TEMP: 기본 예시 Playbook. 실제 서버 환경에서 실행 후 결과 캡처 필요
---
- name: Configure all managed servers
  hosts: all
  become: true

  tasks:
    - name: Update apt cache
      apt:
        update_cache: true

    - name: Install basic packages
      apt:
        name:
          - curl
          - wget
          - git
          - vim
          - net-tools
          - docker.io
        state: present

    - name: Enable and start Docker
      systemd:
        name: docker
        enabled: true
        state: started

- name: Deploy nginx container on web node
  hosts: web
  become: true

  tasks:
    - name: Pull nginx image
      shell: docker pull nginx

    - name: Remove existing nginx container
      shell: docker rm -f web-test
      ignore_errors: true

    - name: Run nginx container
      shell: docker run -d --name web-test -p 80:80 nginx

    - name: Check running containers
      shell: docker ps
      register: docker_ps

    - name: Print container result
      debug:
        var: docker_ps.stdout


