<!-- STATUS: COMPLETE -->

# Team Dandelion 작업일지

## 2026-06-16 작업일지

## 1. 작업 정보

| 구분 | 내용 |
|---|---|
| 작업 일자 | 2026-06-16 |
| 작성자 | 정주헌 |
| 작업 구분 | Phase 기반 범위 재정리, MariaDB 구조 수정, 문서 정비, 아키텍처 다이어그램 정리 |
| 관련 영역 | PM / Architecture, Network, Server, Ansible, Validation, Documentation |

---

## 2. 금일 작업 목표

- 기존 등급 기반 구현 범위를 Phase 기반 로드맵으로 변경
- Phase 1 / Phase 2 / Phase 3 / Out of Scope 기준 확정
- Proxy / Web / DB / Backup Node 분리 구조 확정
- MariaDB 구성 방식을 컨테이너 방식에서 직접 설치 방식으로 수정
- HAProxy Reverse Proxy 및 Load Balancing 적용 범위 정리
- Phase별 아키텍처 다이어그램 작성 및 배치 기준 정리
- SSH 관리 트래픽 방향 정리
- 서브넷 분리 설계 방향 정리
- 멘토링용 압축 작업 순서 문서 작성
- 전체 문서의 오래된 표현 및 중복 표현 검수

---

## 3. 수행 작업

### 3.1 Phase 기반 구현 범위 재정리

기존 등급 기반 범위 표현은 프로젝트 진행 과정에서 범위가 복잡해질 수 있으므로 Phase 기반 구현 로드맵으로 변경하였다.

정리된 기준은 다음과 같다.

| 구분 | 의미 |
|---|---|
| Phase 1 | 필수 구성 및 기본 검증 단계 |
| Phase 2 | 운영 확장 및 검증 고도화 단계 |
| Phase 3 | 도전 확장 단계 |
| Out of Scope | 제외 범위 |

Phase 1은 최종 발표와 제출을 위한 필수 성공 기준으로 정의하였다.  
Phase 2와 Phase 3은 Phase 1이 안정적으로 완료된 이후 진행 가능한 확장 범위로 분리하였다.

---

### 3.2 Phase 1 필수 구성 범위 정리

Phase 1에서는 OpenStack 기반 Ubuntu 인스턴스 위에 역할별 노드를 구성하고, Ansible을 통해 기본 운영 흐름을 자동화하는 방향으로 정리하였다.

Phase 1 필수 흐름은 다음과 같다.

~~~text
OpenStack Ubuntu Instance
→ Control / Proxy / Web / DB / Backup Node 분리
→ SSH 접속 확인
→ Ansible inventory 구성
→ ansible ping 검증
→ Proxy / Web Node Docker 설치
→ DB Node MariaDB 직접 설치 및 서비스 구성
→ Web Node Custom WordPress Container 배포
→ Proxy Node HAProxy HTTP Reverse Proxy 구성
→ Proxy Node 경유 WordPress 접속 확인
→ health_check.sh 실행
→ backup.sh 실행
→ MariaDB dump 및 WordPress files archive 생성
→ restore.md 기반 복구 절차 검증
→ 캡처, 트러블슈팅, 작업일지, 제출 문서 정리
~~~

---

### 3.3 MariaDB 구성 방식 수정

기존 문서 일부에는 MariaDB를 컨테이너로 구성하는 표현이 포함되어 있었다.  
그러나 실제 구현 기준은 DB Node에 MariaDB를 직접 설치하고 systemd 서비스로 운영하는 방식으로 수정하였다.

| 구분 | 기존 방향 | 수정 방향 |
|---|---|---|
| DB 구성 | MariaDB 컨테이너 기반 구성 | MariaDB 직접 설치 |
| 설치 방식 | Docker Compose 기반 DB 실행 | apt 기반 mariadb-server 설치 |
| 서비스 관리 | Docker container 상태 확인 | systemctl mariadb 상태 확인 |
| 백업 방식 | 컨테이너 내부 DB dump | DB Node MariaDB 서비스 대상 dump |
| DB 배치 | Web Node 또는 DB Container 중심 | DB Node 분리 운영 |

최종 기준은 다음과 같다.

~~~text
Proxy Node
→ HAProxy Container

Web Node
→ Custom WordPress Container

DB Node
→ MariaDB Package Install
→ MariaDB systemd Service

Backup / Validation Node
→ health_check.sh
→ backup.sh
→ MariaDB dump
→ WordPress files archive
→ restore.md 검증
~~~

---

### 3.4 HAProxy 적용 범위 정리

HAProxy는 Phase 1부터 Proxy Node에 포함하는 것으로 정리하였다.

Phase별 HAProxy 적용 범위는 다음과 같다.

| Phase | HAProxy 적용 기준 |
|---|---|
| Phase 1 | HTTP Reverse Proxy |
| Phase 2 | HTTPS self-signed, HTTP to HTTPS Redirect |
| Phase 3 | Web Node 1 / Web Node 2 대상 Load Balancing |

Phase 1에서는 Client가 Web Node에 직접 접근하지 않고 Proxy Node를 통해 WordPress 서비스에 접근하는 구조를 검증한다.

~~~text
Client
→ Proxy Node / HAProxy HTTP Reverse Proxy
→ Web Node / Custom WordPress Container
→ DB Node / MariaDB Service
~~~

---

### 3.5 Phase 2 운영 확장 범위 정리

Phase 2는 Phase 1 완료 후 가능한 범위에서 진행하는 운영 확장 단계로 정리하였다.

| 항목 | 기준 |
|---|---|
| HTTPS self-signed | HAProxy에서 HTTPS 접속 검증 |
| HTTP to HTTPS Redirect | 80에서 443으로 Redirect |
| Cinder Backup Volume | Backup Node에 attach 후 backup 저장소로 사용 |
| node_exporter | 서버 OS 메트릭 수집 |
| cAdvisor | 컨테이너 메트릭 수집 |
| Prometheus | 메트릭 수집 |
| Grafana | 대시보드 시각화 |
| backup / restore playbook화 | 운영 절차 자동화 개선 |
| Ansible roles 구조 분리 | 자동화 구조 개선 |

Cinder Volume은 DB 원본 저장소로 사용하지 않고, Backup / Validation Node의 백업 저장소로만 사용한다.

---

### 3.6 Phase 3 도전 확장 범위 정리

Phase 3은 Phase 1과 Phase 2가 조기에 안정화된 경우에만 진행하는 도전 확장 단계로 정리하였다.

Phase 3 목표 구조는 다음과 같다.

~~~text
Client
→ Proxy Node / HAProxy Load Balancer
→ Web Node 1 / WordPress
→ Web Node 2 / WordPress
→ DB Node / MariaDB Service
→ Backup / Validation Node
~~~

검증 기준은 다음과 같다.

| 항목 | 기준 |
|---|---|
| Web Node 2 구성 | 추가 WordPress 서비스 노드 구성 |
| HAProxy Round Robin | Web Node 1 / 2 대상 분산 |
| 공통 DB 연결 | 두 Web Node가 동일 DB Node MariaDB 서비스 사용 |
| 장애 테스트 | Web Node 1 중지 시 Web Node 2 응답 확인 |

WordPress files 자동 동기화, DB Clustering, OpenStack LBaaS / Octavia는 Phase 3 구현 범위에서 제외한다. 단, DB Replication / DB Node 이중화 및 DB Node 이중화는 멘토링 이후 추후 확장 방향으로 검토한다.

---

### 3.7 문서 정비 작업

Phase 기반 구조와 MariaDB 직접 설치 기준에 맞춰 주요 문서를 정비하였다.

정비한 문서는 다음과 같다.

| 문서 | 작업 내용 |
|---|---|
| README.md | Phase 기반 프로젝트 개요 및 최종 구조 반영 |
| docs/scope-control.md | Phase 1 / Phase 2 / Phase 3 / Out of Scope 범위 재정의 |
| docs/implementation-roadmap.md | Phase별 구현 순서 정리 |
| docs/mentoring-brief.md | 멘토링용 프로젝트 현황 요약 정리 |
| docs/mentoring-questions.md | 멘토링 질문 목록 정리 |
| docs/architecture.md | Proxy / Web / DB / Backup 분리 구조 반영 |
| docs/network-design.md | 서브넷, 보안그룹, 통신 흐름 정리 |
| docs/server-setup.md | MariaDB 직접 설치 및 systemd 서비스 기준 반영 |
| docs/ansible-automation.md | MariaDB 설치 자동화 및 WordPress / HAProxy 배포 자동화 반영 |
| docs/validation-report.md | MariaDB dump, WordPress files backup 기준 반영 |
| docs/team-task-guide.md | 팀원별 작업 기준 정리 |
| docs/pre-run-checklist.md | 실행 전 점검 항목 정리 |
| docs/troubleshooting.md | 장애 대응 절차 정리 |
| docs/runbook.md | 실제 실행 절차 정리 |
| docs/final-deliverables.md | 최종 제출 산출물 기준 정리 |
| docs/review-checklist.md | 제출 전 최종 검수 기준 정리 |
| docs/submission-package.md | Google Drive 및 GitHub 제출 패키지 기준 정리 |
| presentation/presentation-outline.md | 발표 흐름을 Phase 기반 구조로 수정 |
| docker/wordpress/README.md | Custom WordPress Image 역할 정리 |

---

### 3.8 Phase별 아키텍처 다이어그램 작성

Phase별로 인프라 중심 다이어그램과 서비스 중심 다이어그램을 분리하여 작성하였다.

총 6장의 Phase별 다이어그램과 1장의 최종 완성본 다이어그램을 기준으로 정리하였다.

| 구분 | 파일명 |
|---|---|
| 최종 아키텍처 완성본 | docs/assets/final-phase3-architecture.png |
| Phase 1 Infrastructure View | docs/assets/phase1-infrastructure-view.png |
| Phase 1 Service View | docs/assets/phase1-service-view.png |
| Phase 2 Infrastructure View | docs/assets/phase2-infrastructure-view.png |
| Phase 2 Service View | docs/assets/phase2-service-view.png |
| Phase 3 Infrastructure View | docs/assets/phase3-infrastructure-view.png |
| Phase 3 Service View | docs/assets/phase3-service-view.png |

발표자료에서는 전체 최종 아키텍처를 먼저 보여주고, 이후 Phase 1 필수 구성과 Phase 3 확장 구조를 중심으로 설명하는 방향으로 정리하였다.

---

### 3.9 최종 아키텍처 다이어그램 수정

최종 Phase 3 아키텍처 다이어그램에서 DNS 구성은 제외하였다.

또한 SSH 관리 통신은 Control Node에서 각 노드로 향하는 방향으로 정리하였다.

~~~text
Control Node
→ SSH / Ansible
→ Proxy Node
→ Web Node 1
→ Web Node 2
→ DB Node
→ Backup / Validation Node
→ Monitoring Node
~~~

트래픽 유형은 다음과 같이 구분하였다.

| 트래픽 유형 | 방향 |
|---|---|
| 사용자 트래픽 | User → Proxy Node → Web Node |
| DB 트래픽 | Web Node → DB Node |
| SSH 관리 트래픽 | Control Node → 각 관리 대상 노드 |
| 백업 트래픽 | Backup Node → Web Node / DB Node |
| 모니터링 트래픽 | Monitoring Node → 수집 대상 노드 |

---

### 3.10 서브넷 분리 설계

프로젝트 설명용 서브넷 설계는 192.168.4.0/24 대역을 /26 단위로 나누는 방향으로 정리하였다.

| Subnet | CIDR | 용도 | 배치 노드 |
|---|---|---|---|
| Management Subnet | 192.168.4.0/26 | SSH / Ansible 관리 | Control Node |
| Service Subnet | 192.168.4.64/26 | 사용자 트래픽 / 웹 서비스 | Proxy Node, Web Node 1, Web Node 2 |
| Database Subnet | 192.168.4.128/26 | DB 통신 전용 | DB Node |
| Operations Subnet | 192.168.4.192/26 | 백업 / 검증 / 모니터링 | Backup Node, Monitoring Node |

설명 기준은 4개 서브넷 분리 구조로 잡되, 실제 구현 중 라우팅 복잡도나 리소스 문제가 발생할 경우 하나의 Private Network 안에서 보안그룹으로 논리 분리하는 방안도 허용하기로 하였다.

---

### 3.11 멘토링용 작업 순서 압축

멘토링에서는 상세 문서 전체를 보여주기보다 압축된 작업 순서를 중심으로 설명하기로 하였다.

멘토링용 작업 순서는 다음과 같다.

~~~text
1. OpenStack에서 Ubuntu 기반 인스턴스 생성
2. Control / Proxy / Web / DB / Backup Node 역할 분리
3. 네트워크, 라우터, 서브넷, 보안그룹 구성
4. Control Node에서 각 노드 SSH 접속 확인
5. Ansible inventory 구성 및 ansible ping 검증
6. Proxy / Web Node에 Docker 설치
7. DB Node에 MariaDB 직접 설치 및 서비스 구성
8. Web Node에 Custom WordPress 컨테이너 배포
9. Web Node의 WordPress가 DB Node MariaDB에 연결되는지 확인
10. Proxy Node에 HAProxy HTTP Reverse Proxy 구성
11. Proxy Node 경유 WordPress 접속 확인
12. health_check.sh로 서비스 상태 점검
13. backup.sh로 MariaDB dump 및 WordPress files 백업
14. restore.md 기준으로 복구 절차 검증
15. 캡처, 트러블슈팅, 작업일지, 제출 문서 정리
~~~

---

### 3.12 문서 표현 검수

문서 전체에서 오래된 구조나 현재 기준과 맞지 않는 표현을 제거하였다.

검수 대상 표현은 다음과 같다.

~~~text
기존 등급 명칭
MariaDB Container
MariaDB 컨테이너
dandelion-mariadb
docker/compose/db
mariadb:10.11
Nginx
nginx
web-test
중복된 mysqldump 표현
~~~

최종 표현은 다음 기준으로 통일하였다.

| 항목 | 최종 표현 |
|---|---|
| DB 구성 | DB Node MariaDB Service |
| DB 설치 | MariaDB 직접 설치 및 서비스 구성 |
| DB 상태 | mariadb service active |
| DB 백업 | MariaDB dump |
| Web 구성 | Custom WordPress Container |
| Proxy 구성 | HAProxy Container |
| 범위 표현 | Phase 1 / Phase 2 / Phase 3 / Out of Scope |

---

## 4. 금일 결정 사항

| 번호 | 결정 내용 |
|---:|---|
| 1 | 기존 등급 기반 구현 범위 표현을 Phase 기반 로드맵으로 변경한다. |
| 2 | Phase 1은 최종 발표 및 제출의 필수 성공 기준으로 둔다. |
| 3 | Phase 2는 HTTPS, Cinder, Monitoring, Playbook 개선 등 운영 확장으로 둔다. |
| 4 | Phase 3은 Web Node 2대와 HAProxy Load Balancing 도전 확장으로 둔다. |
| 5 | Proxy Node는 HAProxy 컨테이너로 구성한다. |
| 6 | Web Node는 Custom WordPress 컨테이너로 구성한다. |
| 7 | DB Node는 MariaDB를 컨테이너가 아니라 직접 설치하여 systemd 서비스로 운영한다. |
| 8 | Backup / Validation Node는 health_check.sh, backup.sh, MariaDB dump, restore.md 검증을 담당한다. |
| 9 | Cinder Volume은 DB 원본 저장소가 아니라 Backup Node 백업 저장소로 사용한다. |
| 10 | 최종 아키텍처 다이어그램에서 DNS 구성은 제외한다. |
| 11 | SSH 관리 트래픽은 Control Node에서 각 노드로 향하는 방향으로 표시한다. |
| 12 | 192.168.4.0/24 대역은 /26 기준 4개 서브넷으로 나누는 설계를 사용한다. |
| 13 | 실제 구현이 복잡할 경우 단일 Private Network와 보안그룹 기반 논리 분리로 대체할 수 있다. |
| 14 | 멘토링 자료는 상세 문서 전체가 아니라 압축된 작업 순서 중심으로 보여준다. |

---

## 5. 산출물 변경 사항

| 산출물 | 변경 내용 |
|---|---|
| README.md | Phase 기반 구조 및 최종 아키텍처 반영 |
| docs/architecture.md | Proxy / Web / DB / Backup 분리 구조 반영 |
| docs/network-design.md | 4개 서브넷 설계 및 보안그룹 기준 반영 |
| docs/scope-control.md | Phase 1 / Phase 2 / Phase 3 기준 반영 |
| docs/server-setup.md | MariaDB 직접 설치 기준 반영 |
| docs/ansible-automation.md | DB Node MariaDB 설치 자동화 기준 반영 |
| docs/validation-report.md | Phase별 검증 기준 반영 |
| docs/mentoring-work-order-summary.md | 멘토링용 압축 작업 순서 추가 |
| presentation/presentation-outline.md | 발표자료 이미지 배치 기준 반영 |
| docs/assets/ | Phase별 아키텍처 이미지 정리 예정 |

---

## 6. 이슈 및 대응

| 이슈 | 내용 | 대응 |
|---|---|---|
| MariaDB 구성 혼선 | 기존 문서 일부에 MariaDB 컨테이너 표현 존재 | DB Node 직접 설치 및 systemd 서비스 기준으로 수정 |
| 범위 표현 혼선 | 기존 등급 기반 표현이 일부 문서에 남아 있음 | Phase 기반 표현으로 통일 |
| mysqldump 표현 중복 | 일괄 치환 과정에서 중복 표현 발생 | MariaDB dump 표현으로 정리 |
| 아키텍처 다이어그램 과밀 | Phase 1/2/3가 한 장에 모두 들어가 가독성 저하 | Phase별 인프라/서비스 그림으로 분리 |
| DNS 표현 불필요 | 실제 프로젝트 범위에 DNS가 포함되지 않음 | 최종 아키텍처에서 DNS 제거 |
| SSH 화살표 방향 혼선 | SSH 관리선이 사용자 트래픽처럼 보일 수 있음 | Control Node → 각 노드 방향으로 수정 |
| 서브넷 구현 복잡도 | 4개 서브넷 구현 시 라우팅이 복잡해질 수 있음 | 실제 구현은 단일 Private Network + 보안그룹 분리도 허용 |

---

## 7. 다음 작업

| 담당자 | 다음 작업 |
|---|---|
| 정주헌 | Phase 기반 문서 최종 검수, MariaDB 표현 정리, 아키텍처 이미지 배치, 발표 흐름 정리 |
| 백서빈 | OpenStack 인스턴스, 네트워크, 서브넷, 라우터, 보안그룹 구성 |
| 이진욱 | Proxy Node HAProxy, Web Node WordPress, DB Node MariaDB 직접 설치 구성 지원 |
| 조민석 | Ansible inventory, site.yml, MariaDB 설치 자동화, WordPress / HAProxy 배포 자동화 |
| 박재우 | health_check.sh, backup.sh, MariaDB dump, restore.md 검증, 모니터링 확장 검토 |

---

## 8. 작업 결과 요약

금일 작업을 통해 프로젝트 범위를 기존 등급 기반 표현에서 Phase 기반 구현 로드맵으로 변경하였다.

Phase 1은 OpenStack 인프라 구성, Control / Proxy / Web / DB / Backup Node 분리, Ansible 자동화, Custom WordPress 배포, MariaDB 직접 설치, HAProxy HTTP Reverse Proxy, 상태 점검, 백업, 복구 검증까지를 필수 성공 기준으로 확정하였다.

Phase 2는 HTTPS, Cinder Backup Volume, Prometheus/Grafana, Playbook 개선 등 운영 확장으로 정리하였다.

Phase 3은 Web Node 2대와 HAProxy Load Balancing을 통한 도전 확장으로 정리하였다.

또한 MariaDB는 컨테이너가 아니라 DB Node에 직접 설치하고 systemd 서비스로 운영하는 것으로 기준을 수정하였다. 최종 아키텍처 다이어그램에서는 DNS를 제외하고, SSH 관리 트래픽은 Control Node에서 각 노드로 향하도록 표시하기로 하였다.

---

## 9. 추가 작업: 최종 아키텍처 이미지 배치 기준 정리

Phase별 아키텍처 다이어그램은 인프라 중심과 서비스 중심으로 분리하여 작성하고, 최종 완성본은 Phase 3 기준 전체 구조로 정리하였다.

이미지 배치 기준은 다음과 같다.

| 이미지 | 배치 위치 |
|---|---|
| final-phase3-architecture.png | README.md, docs/architecture.md, 발표자료 전체 아키텍처 슬라이드 |
| phase1-infrastructure-view.png | docs/architecture.md, docs/network-design.md, 발표자료 Phase 1 인프라 슬라이드 |
| phase1-service-view.png | docs/server-setup.md, docs/validation-report.md, 발표자료 Phase 1 서비스 슬라이드 |
| phase2-infrastructure-view.png | docs/implementation-roadmap.md, docs/network-design.md |
| phase2-service-view.png | docs/validation-report.md, docs/final-deliverables.md |
| phase3-infrastructure-view.png | docs/architecture.md, docs/network-design.md |
| phase3-service-view.png | docs/validation-report.md, presentation/presentation-outline.md |

발표자료에서는 전체 최종 아키텍처를 먼저 보여주고, 이후 Phase 1 필수 구성과 Phase 3 확장 구조를 중심으로 설명한다.

---

## 10. 추가 작업: 서브넷 설계 반영

프로젝트 설명용 서브넷 설계는 192.168.4.0/24 대역을 /26 단위로 나누는 방향으로 정리하였다.

| Subnet | CIDR | 용도 | 배치 노드 |
|---|---|---|---|
| Management Subnet | 192.168.4.0/26 | SSH / Ansible 관리 | Control Node |
| Service Subnet | 192.168.4.64/26 | 사용자 트래픽 / 웹 서비스 | Proxy Node, Web Node 1, Web Node 2 |
| Database Subnet | 192.168.4.128/26 | DB 통신 전용 | DB Node |
| Operations Subnet | 192.168.4.192/26 | 백업 / 검증 / 모니터링 | Backup Node, Monitoring Node |

설계상으로는 관리 트래픽, 사용자 서비스 트래픽, DB 트래픽, 운영 검증 트래픽을 분리한다.

실제 구현 중 라우팅 복잡도나 리소스 문제가 발생할 경우, 하나의 Private Network 안에서 보안그룹으로 논리 분리하는 방식으로 대체할 수 있다.

---

## 11. 추가 작업: 멘토링용 압축 자료 작성

멘토링 시에는 상세 문서 전체를 보여주기보다, 작업 순서를 압축해서 보여주는 방향으로 정리하였다.

작성 대상 문서는 다음과 같다.

~~~text
docs/mentoring-work-order-summary.md
~~~

주요 내용은 다음과 같다.

| 구분 | 내용 |
|---|---|
| Phase 1 | 필수 구성 및 기본 검증 |
| Phase 2 | 운영 확장 및 검증 고도화 |
| Phase 3 | 도전 확장 |
| Out of Scope | 제외 범위 |
| 멘토링 질문 | 범위, DB 구성, HAProxy, Cinder, Monitoring, LB 확장 적절성 확인 |

---

## 12. 추가 작업: 문서 검수 명령어 정리

문서 내 오래된 표현과 중복 표현을 검수하기 위해 다음 기준을 정리하였다.

~~~powershell
Select-String -Path README.md, docs\*.md, presentation\*.md, docker\wordpress\README.md -Pattern "A급|B급|B\+|C급|MariaDB 컨테이너|MariaDB Container|dandelion-mariadb|docker/compose/db|mariadb:10.11|Nginx|nginx|web-test|mysqldump 기반 mysqldump"
~~~

검수 후 다음 기준으로 표현을 통일한다.

| 항목 | 최종 표현 |
|---|---|
| 범위 표현 | Phase 1 / Phase 2 / Phase 3 / Out of Scope |
| DB 구성 | DB Node MariaDB Service |
| DB 설치 | MariaDB 직접 설치 및 서비스 구성 |
| DB 상태 | mariadb service active |
| DB 백업 | MariaDB dump |
| Web 구성 | Custom WordPress Container |
| Proxy 구성 | HAProxy Container |


---

## 13. 추가 작업: OpenStack 인스턴스 리소스 산정 반영

Phase 1 기본 구현에 필요한 OpenStack 인스턴스별 리소스 산정표를 정리하였다.

기본 노드는 Control, HAProxy, Web, DB, Backup Node로 분리하며, 각 노드 역할에 따라 vCPU, Disk, RAM을 다르게 배정하였다.

| Node | Role | vCPU | Disk | RAM |
|---|---|---:|---:|---:|
| control | Ansible Control Node | 2 Core | 20 GB | 2 GB |
| haproxy | HAProxy Reverse Proxy Node | 1 Core | 15 GB | 1 GB |
| web | Custom WordPress Web Node | 2 Core | 20 GB | 2 GB |
| db | MariaDB Service Node | 2 Core | 20 GB | 4 GB |
| backup | Backup / Validation Node | 1 Core | 80 GB | 2 GB |

기본 구성 전체 리소스는 다음과 같다.

| Resource | Total |
|---|---:|
| vCPU | 8 Core |
| Disk | 155 GB |
| RAM | 11 GB |

### 13.1 리소스 산정 기준

| Node | 산정 이유 |
|---|---|
| control | Ansible inventory 관리, playbook 실행, SSH 기반 원격 구성 수행 |
| haproxy | HAProxy Reverse Proxy 역할만 수행하는 경량 노드 |
| web | Custom WordPress Container 실행 및 HTTP 서비스 제공 |
| db | MariaDB 직접 설치 및 systemd 서비스 운영, DB 처리 안정성을 위해 RAM 4 GB 배정 |
| backup | MariaDB dump, WordPress files archive, restore 검증 자료 보관을 위해 Disk 80 GB 배정 |

### 13.2 Phase 확장 기준

Phase 3에서 Web Node 2를 추가할 경우 다음 리소스가 추가로 필요하다.

| Additional Node | vCPU | Disk | RAM |
|---|---:|---:|---:|
| web-2 | 2 Core | 20 GB | 2 GB |

Phase 3 확장 시 전체 예상 리소스는 다음과 같다.

| Resource | Total |
|---|---:|
| vCPU | 10 Core |
| Disk | 175 GB |
| RAM | 13 GB |



---

## 14. 추가 작업: 멘토링 결과 기반 DB Node 이중화 방향 반영

멘토링 진행 결과, 현재 구현 범위에서는 단일 DB Node를 유지하되 추후 확장 방향으로 DB Node 이중화를 검토하기로 정리하였다.

기존 구조는 Web Node가 단일 DB Node의 MariaDB Service에 연결되는 방식이다.

~~~text
Web Node
→ DB Node / MariaDB Service
→ Backup / Validation Node
~~~

멘토링 이후 확장 방향은 다음과 같이 정리하였다.

~~~text
Web Node 1 / Web Node 2
→ DB Node 1 / Primary MariaDB
→ DB Node 2 / Replica MariaDB
→ Backup / Validation Node
~~~

### 14.1 현재 구현 범위

| 항목 | 기준 |
|---|---|
| DB Node | 단일 DB Node |
| DB 서비스 | MariaDB 직접 설치 및 systemd 운영 |
| DB 접근 | Web Node에서 DB Node 3306 접근 |
| 백업 | Backup Node에서 MariaDB dump 수행 |
| 복구 | restore.md 기준 수동 복구 절차 검증 |

### 14.2 추후 확장 방향

| 항목 | 기준 |
|---|---|
| DB Node 이중화 | Primary / Replica 구조 검토 |
| 데이터 복제 | MariaDB Replication 검토 |
| 장애 대응 | DB 장애 시 Replica 기반 전환 구조 검토 |
| 모니터링 | DB 상태 수집 및 장애 감지 연계 |
| 백업 고도화 | Dump 백업과 이중화 구조를 함께 활용 |

### 14.3 문서 반영 기준

DB Node 이중화는 현재 Phase 1~3 구현 범위에 즉시 포함하지 않는다.

대신 최종 발표와 결과보고서에서는 다음 항목에 포함한다.

| 문서 위치 | 반영 내용 |
|---|---|
| 발표자료 한계 및 개선 방향 | 단일 DB Node 구조의 한계와 DB 이중화 필요성 |
| 최종 보고서 향후 개선 방향 | MariaDB Replication / DB Failover 검토 |
| architecture 문서 | Post-Phase Extension 항목으로 DB Node 이중화 명시 |
| validation 문서 | 현재는 Backup / Restore 검증, 추후 이중화 검증 가능성 설명 |

최종 정리 기준은 다음과 같다.

~~~text
현재 구현:
단일 DB Node + MariaDB Service + MariaDB dump + restore.md 검증

추후 개선:
DB Node 이중화 + MariaDB Replication + DB Failover 검토
~~~


---

## 15. 추가 작업: 팀원별 구축 작업 현황 반영

금일은 Phase 기반 구조 정리 이후 각 담당자별 실제 구축 작업을 진행하였다.

전체적으로는 문서 정리보다 실제 OpenStack 인프라 구성, 서버 초기 환경 구성, Ansible 자동화 초안, 검증 및 백업 스크립트 작성 작업이 중심이었다.

---

### 15.1 백서빈 작업 내용

백서빈은 OpenStack 인프라 담당으로 보안그룹 및 서브넷 구성을 진행하였다.

현재 4개 서브넷 중 3개는 생성 및 기본 검증을 완료하였다.

Backup / Validation 영역에 해당하는 서브넷 또는 인스턴스가 정상적으로 올라가지 않는 문제가 발생하여 조치 중이다.

| 작업 항목 | 상태 |
|---|---|
| 보안그룹 생성 | 완료 |
| 서브넷 생성 | 4개 중 3개 완료 |
| 인스턴스 생성 | 일부 완료 |
| 검증 / 복구용 인스턴스 | 생성 또는 부팅 문제 조치 중 |
| 후속 작업 | 네트워크, 라우터, 보안그룹, flavor 상태 확인 |

---

### 15.2 이진욱 작업 내용

이진욱은 각 노드별 서버 초기 환경 구성 기준을 정리하였다.

노드별 리소스 기준은 다음과 같다.

| Node | vCPU | Disk | RAM |
|---|---:|---:|---:|
| control | 2 Core | 20 GB | 2 GB |
| haproxy | 1 Core | 15 GB | 2 GB |
| web | 2 Core | 20 GB | 2 GB |
| db | 2 Core | 20 GB | 4 GB |
| backup | 1 Core | 80 GB | 2 GB |

공통 초기 설정은 다음과 같다.

~~~text
sudoers 파일에 user1 NOPASSWD 권한 추가
모든 노드 공통 패키지 설치
- vim
- curl
- wget
~~~

노드별 설치 기준은 다음과 같다.

| Node | 설치 항목 |
|---|---|
| control | ansible |
| haproxy | haproxy 직접 설치 또는 docker 기반 HAProxy Container 실행 환경 |
| web | docker, docker compose plugin |
| db | mariadb-server |
| backup | mariadb-client |

DB Node는 mariadb-server를 설치하고 systemctl로 mariadb 서비스를 enable/start하는 구조로 정리하였다.

Backup Node는 DB Node에 접속하여 MariaDB dump를 수행할 수 있도록 mariadb-client를 설치한다.

---

### 15.3 조민석 작업 내용

조민석은 Ansible 자동화 담당으로 inventory.ini와 ansible.cfg 초안을 작성하였다.

inventory.ini는 proxy, web, db, backup 그룹을 분리하고 managed 그룹으로 묶는 구조이다.

~~~text
[proxy]
proxy-node ansible_host=PROXY_NODE_PRIVATE_IP

[web]
web-node ansible_host=WEB_NODE_PRIVATE_IP

[db]
db-node ansible_host=DB_NODE_PRIVATE_IP

[backup]
backup-node ansible_host=BACKUP_NODE_PRIVATE_IP

[managed:children]
proxy
web
db
backup
~~~

ansible.cfg는 다음 기준으로 작성하였다.

| 항목 | 기준 |
|---|---|
| inventory | ./inventory.ini |
| host_key_checking | False |
| remote_user | ubuntu |
| private_key_file | ~/.ssh/dandelion-key.pem |
| become | True |
| become_user | root |
| pipelining | True |

후속 작업은 실제 OpenStack 인스턴스의 Private IP를 inventory.ini에 반영하고, Control Node에서 ansible ping을 검증하는 것이다.

~~~text
ansible all -m ping
ansible managed -m ping
~~~

---

### 15.4 박재우 작업 내용

박재우는 검증 및 백업 담당으로 healthcheck-example.sh와 backup.sh 초안을 작성하였다.

healthcheck-example.sh는 다음 대상에 대해 상태 점검을 수행하는 구조이다.

| 대상 | 점검 방식 |
|---|---|
| web1 | ping, curl, container health |
| web2 | ping, curl, container health |
| db | ping, DB service status |
| proxy | ping, proxy service status |
| backup | ping, disk usage check |

backup.sh는 DB Node에 SSH 접속하여 WordPress DB를 dump하고 날짜별 백업 디렉터리에 SQL 파일을 저장하는 구조로 작성하였다.

현재 초안은 다음 기준으로 보완이 필요하다.

| 항목 | 보완 내용 |
|---|---|
| DB 서비스명 | mysqld 대신 mariadb 기준으로 수정 |
| Proxy 점검 | HAProxy Container 기준으로 점검 방식 확정 |
| web2 재시작 | web2 노드에서 재시작하도록 수정 |
| 백업 경로 | /tmp 대신 /backup 또는 제출 기준 경로 사용 |
| DB 인증정보 | 변수화 또는 테스트 계정 기준 정리 |
| WordPress files | SQL dump 외 파일 archive 추가 필요 |

---

### 15.5 금일 작업 결과 요약

금일 작업에서는 백서빈이 OpenStack 보안그룹과 서브넷 구성을 진행하였고, 4개 서브넷 중 3개 구성을 완료하였다.

검증 및 복구 영역의 서브넷 또는 인스턴스에서 생성 문제가 발생하여 원인 조치 중이다.

이진욱은 각 노드별 리소스 기준과 패키지 설치 기준을 정리하였다.

조민석은 Ansible inventory.ini와 ansible.cfg 초안을 작성하였다.

박재우는 healthcheck-example.sh와 backup.sh 초안을 작성하였으며, 현재 아키텍처 기준에 맞게 서비스명, 백업 경로, 컨테이너 점검 방식 등을 보완할 예정이다.

